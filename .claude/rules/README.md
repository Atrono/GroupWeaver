# Path-scoped rules

Evolving, durable knowledge about this codebase lands here as small `*.md`
files (CLAUDE.md "Memory discipline") - quirks, gotchas, decisions too small
for an ADR. Verify the harness actually loads this directory (if not, reference
files from CLAUDE.md via @-imports instead).

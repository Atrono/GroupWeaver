# 2026-06-22 - Session 22 (v0.2 polish pass - Phase 1: shipped the 6-issue work front)

**Phase:** Phase 4 (v0.2 polish pass). Executed the entire Phase-1 backlog from the
2026-06-21 audit as six vertical slices, each branch -> implementer/test-engineer ->
build.ps1 + (graph/ui) ui-verifier/test-graph-bundle -> reviewer APPROVE -> squash PR
-> ci-sentinel green -> issue closed. (#95 was already merged last session and needed
nothing.) All six green on CI; `main` now at cfdb36f.

- **#94 graph in-canvas busy ring (PR #99, ADR-019).** `IGraphRenderer.SetBusyAsync`
  default no-op; fire-and-forget dispatch (no single-flight - would deadlock the
  in-flight expand - no TCS, never FocusCalls, never-throw). graph.js `node[busy][!sev]`
  overlay #4FA3E3/0.35/8 after severity+roll-up; transient flag self-clears on
  graphUpdate, `updateStyle()` clears it on the fetch-fail/cancel path. Wired in
  ExpandAsync fetch-path only. test-engineer caught a real cytoscape overlay-opacity
  freeze and fixed it.
- **#96 reverse sidebar->graph selection sync (PR #100, ADR-020).** VM->JS `select`
  command reusing applySelection/clearSelection (byte-identical to a tap, instant).
  Its OWN FakeGraphRenderer.SelectCalls channel - the JumpCommand FocusAsync-exactly-once
  pin stays green. Wired from OnSelectedDnChanged (every selection source syncs).
- **#90 design tokens + WCAG re-tone (PR #101, ADR-021).** New `BrandTokens` = the
  declared single source; the 5 App converters reference it; graph.js/index.html/
  verify.mjs/WebBundleTests stay tripwire-pinned mirrors (review-only parity invariant).
  Fixed white-on-Warning 2.06 / white-on-Info 2.73 via per-hue `ToTextBrush` (dark ink),
  and node 1.4.11 via a 2px #8A93A3 border-lift on DL/UG/Computer (no fill hex moved).
  check-contrast.ps1 updated to the shipped state.
- **#91 type scale & voice (PR #102).** Styles/Tokens.axaml + Typography.axaml (12 class
  levels + Button.ghost) - the chrome foundation #92/#93 consume. Wordmark weight-split
  signature (Group=Light/Weaver=SemiBold); monospace DNs+SAM. TypographyTests bite-pin it.
- **#93 connect + settings hierarchy (PR #103).** Connect=accent primary / Demo=ghost in
  a token card with eyebrow + concentric-ring signature; settings Save=accent /
  Apply-Cancel=ghost (Content unchanged); naming cards re-skinned.
- **#92 workspace rhythm (PR #104).** Findings OpacityMask bottom-fade (clip reads
  scrollable), unchecked-hint info block (chrome#10), detail terminus + Spacing 10
  (the 1920 gutter reads intentional).

**Decided.** Node 1.4.11 fix = border-lift, not brightening (keeps fill hexes + kind-badge
white-text). `Space*` tokens are `x:Double` -> bind only to Spacing/Width, never
Padding/Margin (Thickness) - use literals (caught in #93, applied in #92).

- **Checklist elevated (PR #105, d5e007a).** `docs/ui-checklist.md` now pins the v0.2 bar
  (WCAG 2.2 AA + identity + motion): a cross-cutting "Visual identity & type" section + the
  busy-ring / reverse-sync / contrast-lift items + the per-surface elevations. reviewer
  verified every item against the merged code.
- **Plan/Gap identity propagated (#106, PR #107, 1b3ba6a).** Brought `PlanView`/`GapView`
  onto the type scale + per-hue ink — the last surfaces off the bar. New
  `BrandTokens.OnLightTextStrong` (#000000): the diff mid-tone Removed `#E0503A` reaches only
  4.23:1 with the page-bg ink, so the diff badges need black (5.38:1). Resolves the ADR-021
  GapView deferral; `GapKindConvertersTests` pins it.

- **Demo media refreshed (#108, 9f31cb1).** Regenerated `docs/media/graph-explore.png` +
  `gap-analysis.png` (from verify.mjs graph-focus/graph-diff — now the #87 legend + #90
  contrast-lift rings) and re-recorded `m2-explore.gif` (eased focus/enter-fade/selection
  halo + mono DN + unchecked info block). The larger #87 legend occluded the rust root at
  the 2-node scope and timed out the recorder's pixel-hunt; fixed with a new
  `Send-CanvasDrag` (cytoscape pan) in `tools/lib/webview-capture.ps1` + a beat-3 restructure
  (gate on the External node, pan the root clear of the legend, then hunt). Demo-only intact.
- **v0.2.1 RELEASED (#109, e76d5fc, tag `v0.2.1`).** Cut on the user's tag call: bumped
  `Directory.Build.props` 0.2.0→0.2.1 + the SmokeTests pin + README + CHANGELOG `[0.2.1]`.
  Pre-flight `pack-release.ps1 -Version 0.2.1` green (exe reports "GroupWeaver 0.2.1"),
  `/security-review` CLEAN over the whole v0.2..HEAD delta (busy/select bridge DNs ASCII-escaped
  via GraphJson + getElementById-only; #45 plain-text intact; PlanScriptExporter guard intact).
  `release.yml` published the portable zip + sha256 + SLSA build-provenance attestation; verified
  the public zip end-to-end (SHA256 5034ac38… matches the sidecar; `gh attestation verify` exit 0).
  https://github.com/Atrono/GroupWeaver/releases/tag/v0.2.1

- **CI format-flake fixed (#110, a41672f).** The `dotnet format` "Unable to locate dotnet CLI"
  flake (format resolves the SDK via DOTNET_ROOT, intermittently unset after setup-dotnet on
  windows-2022) is cured by a new ci.yml step pinning `DOTNET_ROOT` (derived from
  `Get-Command dotnet` → `C:\Program Files\dotnet`, exported via GITHUB_ENV). Verified in the
  PR run: the pin propagated into the Build gate env and the format step passed clean.

**Next.** v0.2 polish pass COMPLETE and shipped as v0.2.1; CI format-flake fixed. Remaining
audit items are Medium/Low (edge legibility F6/F7, interactive legend filter F10, German-DN
picker fixture) — no active work item pending.

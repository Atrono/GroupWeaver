# 2026-06-22 - Session 22 (v0.2 polish pass COMPLETE - shipped end-to-end as v0.2.1)

**Phase:** Phase 4 (v0.2 polish pass) - full arc in one session: shipped the entire
Phase-1 audit backlog as six vertical slices, did the Phase-2 consolidation (ui-checklist
+ Plan/Gap propagation + demo-media refresh), cut and verified the public **v0.2.1**
release, and hardened CI. Each code slice ran branch -> implementer/test-engineer ->
build.ps1 + (graph/ui) ui-verifier/test-graph-bundle -> reviewer APPROVE -> squash PR
-> ci-sentinel green -> issue closed. (#95 was already merged last session and needed
nothing.) `main` at f0a4db8; all green on CI.

## Done

### Phase-1 slices (the 6-issue audit front)

- **#94 graph in-canvas busy ring (PR #99, ADR-019).** `IGraphRenderer.SetBusyAsync`
  default no-op; fire-and-forget dispatch (no single-flight - would deadlock the
  in-flight expand - no TCS, never FocusCalls, never-throw). graph.js `node[busy][!sev]`
  overlay #4FA3E3/0.35/8 after severity+roll-up; transient flag self-clears on
  graphUpdate, `updateStyle()` clears it on the fetch-fail/cancel path. Wired in
  ExpandAsync fetch-path only. test-engineer caught a real cytoscape overlay-opacity
  freeze and fixed it.
- **#96 reverse sidebar->graph selection sync (PR #100, ADR-020).** VM->JS `select`
  command reusing applySelection/clearSelection (byte-identical to a tap, instant).
  Its OWN FakeGraphRenderer.SelectCalls channel - the JumpCommand FocusAsync-exactly-once
  pin stays green. Wired from OnSelectedDnChanged (every selection source syncs).
- **#90 design tokens + WCAG re-tone (PR #101, ADR-021).** New `BrandTokens` = the
  declared single source; the 5 App converters reference it; graph.js/index.html/
  verify.mjs/WebBundleTests stay tripwire-pinned mirrors (review-only parity invariant).
  Fixed white-on-Warning 2.06 / white-on-Info 2.73 via per-hue `ToTextBrush` (dark ink),
  and node 1.4.11 via a 2px #8A93A3 border-lift on DL/UG/Computer (no fill hex moved).
  check-contrast.ps1 updated to the shipped state.
- **#91 type scale & voice (PR #102).** Styles/Tokens.axaml + Typography.axaml (12 class
  levels + Button.ghost) - the chrome foundation #92/#93 consume. Wordmark weight-split
  signature (Group=Light/Weaver=SemiBold); monospace DNs+SAM. TypographyTests bite-pin it.
- **#93 connect + settings hierarchy (PR #103).** Connect=accent primary / Demo=ghost in
  a token card with eyebrow + concentric-ring signature; settings Save=accent /
  Apply-Cancel=ghost (Content unchanged); naming cards re-skinned.
- **#92 workspace rhythm (PR #104).** Findings OpacityMask bottom-fade (clip reads
  scrollable), unchecked-hint info block (chrome#10), detail terminus + Spacing 10
  (the 1920 gutter reads intentional).

### Consolidation, release & CI

- **Checklist elevated (PR #105, d5e007a).** `docs/ui-checklist.md` now pins the v0.2 bar
  (WCAG 2.2 AA + identity + motion): a cross-cutting "Visual identity & type" section + the
  busy-ring / reverse-sync / contrast-lift items + the per-surface elevations. reviewer
  verified every item against the merged code.
- **Plan/Gap identity propagated (#106, PR #107, 1b3ba6a).** Brought `PlanView`/`GapView`
  onto the type scale + per-hue ink — the last surfaces off the bar. New
  `BrandTokens.OnLightTextStrong` (#000000): the diff mid-tone Removed `#E0503A` reaches only
  4.23:1 with the page-bg ink, so the diff badges need black (5.38:1). Resolves the ADR-021
  GapView deferral; `GapKindConvertersTests` pins it.
- **Demo media refreshed (#108, 9f31cb1).** Regenerated `docs/media/graph-explore.png` +
  `gap-analysis.png` (from verify.mjs graph-focus/graph-diff — now the #87 legend + #90
  contrast-lift rings) and re-recorded `m2-explore.gif` (eased focus/enter-fade/selection
  halo + mono DN + unchecked info block). The larger #87 legend occluded the rust root at
  the 2-node scope and timed out the recorder's pixel-hunt; fixed with a new
  `Send-CanvasDrag` (cytoscape pan) in `tools/lib/webview-capture.ps1` + a beat-3 restructure
  (gate on the External node, pan the root clear of the legend, then hunt). Demo-only intact.
- **v0.2.1 RELEASED (#109, e76d5fc, tag `v0.2.1`).** Cut on the user's tag call: bumped
  `Directory.Build.props` 0.2.0→0.2.1 + the SmokeTests pin + README + CHANGELOG `[0.2.1]`.
  Pre-flight `pack-release.ps1 -Version 0.2.1` green (exe reports "GroupWeaver 0.2.1"),
  `/security-review` CLEAN over the whole v0.2..HEAD delta (busy/select bridge DNs ASCII-escaped
  via GraphJson + getElementById-only; #45 plain-text intact; PlanScriptExporter guard intact).
  `release.yml` published the portable zip + sha256 + SLSA build-provenance attestation; verified
  the public zip end-to-end (SHA256 5034ac38… matches the sidecar; `gh attestation verify` exit 0).
  https://github.com/Atrono/GroupWeaver/releases/tag/v0.2.1
- **CI format-flake fixed (#110, a41672f).** The `dotnet format` "Unable to locate dotnet CLI"
  flake (format resolves the SDK via DOTNET_ROOT, intermittently unset after setup-dotnet on
  windows-2022) is cured by a new ci.yml step pinning `DOTNET_ROOT` (derived from
  `Get-Command dotnet` → `C:\Program Files\dotnet`, exported via GITHUB_ENV). Verified in the
  PR run: the pin propagated into the Build gate env and the format step passed clean.

## Decided

- **Node 1.4.11 fix = border-lift (#8A93A3), not brightening** — keeps every fill hex AND the
  kind-badge white-text contrast intact (brightening sat on the 4.5:1 edge).
- **The diff palette's mid-tone Removed #E0503A clears 4.5:1 with NEITHER white nor the page-bg
  ink** (#1b1f27 = 4.23) — so the Gap badges use pure black (`BrandTokens.OnLightTextStrong`),
  unlike the severity badges (#1b1f27).
- **`Space*` tokens are `x:Double`** → bind only to Spacing/Width, never Padding/Margin
  (`Thickness`, throws InvalidCast) — use literals (caught #93, applied #92/#106).
- **The bigger #87 legend occludes the 2-node-scope root in the GIF** — pan the graph clear
  before the pixel-hunt (new `Send-CanvasDrag`), rather than shrinking the legend.
- **`dotnet format` resolves the SDK via `DOTNET_ROOT`, not the PATH muxer** — pin it
  (derived from `Get-Command dotnet`) right after setup-dotnet on Windows runners.

**Next.** v0.2 polish pass COMPLETE and shipped as v0.2.1; CI format-flake fixed. Remaining
audit items are Medium/Low (edge legibility F6/F7, interactive legend filter F10, German-DN
picker fixture) — no active work item pending.

# 2026-06-22 — Session 23 (skill-authoring: four new on-demand skills, TDD-verified)

**Phase:** Phase 4 (tooling/meta — no product code). Answered "what skills should I
create?" by mining the codebase/journals for repeated, intricate, judgment-heavy
procedures currently re-derived or scattered, then authored four `.claude/skills/`
via the `writing-skills` RED→GREEN discipline (baseline agent without the skill →
write minimal skill → fresh agent with the skill). On branch `docs/add-four-skills`.

## Done

- **Four skills, each RED→GREEN verified** (one commit each): `cut-release`,
  `record-demo-media`, `ad-fixture-design`, `security-review-groupweaver`. Every
  baseline agent reconstructed most of its runbook from the self-documenting
  scripts but at 75k–152k tokens AND missed or were unsure on the institutional
  gotchas; the skill-equipped re-run nailed them at ~37k tokens. `reviewer`
  APPROVE (fact-checked all four against source; confirmed the `NamingPreview`
  construction length-cap gap the security skill flags is a real live finding).
- **Headline gaps each skill closes:** cut-release — DOTNET_ROOT CI flake +
  tag-on-the-bump-commit + both-streams smoke deadlock; record-demo-media — the
  undocumented verify.mjs frame → docs/media copy/rename mapping (baseline's
  "single biggest risk"); ad-fixture-design — the live-AD test count-pin sync map
  + CI-skips-AD-tests; security-review-groupweaver — the recurring finding-classes
  (smart-quote breakout, guard-predicate drift, relaxed-encoder regression).

## Decided

- **Skills here are deliberately project-specific** (intricate procedures +
  institutional memory), distinct from generic techniques — matching the existing
  `agdlp-domain`/`headless-uitest` precedent. Always-on gotchas stay in
  `.claude/rules/` (kept `lab-environment.md` a rule, not a skill); the new skills
  cross-reference it.

**Next.** PR for the four-skill branch → reviewer/CI green → merge. No product work
pending; remaining v0.2 audit items still Medium/Low.

# 2026-06-22 — Session 24 (#112 fix — the skills' first dividend: shared regex length cap)

**Phase:** Phase 4. Merged the four-skill branch (PR #111 → e4dd091), then closed the
loop on the first real finding those skills surfaced: filed and fixed **#112** end-to-end
(branch → test-engineer RED → implementer GREEN → reviewer APPROVE → CI green → squash
PR #113 → 9dec1f5, #112 closed). `main` green.

## Done

- **#112 — NamingPreview construction-DoS gap (PR #113).** The settings live-preview
  compiled an untrusted naming regex with `NonBacktracking` and NO length cap before
  `new Regex(...)` — a DFA whose *construction* cost scales with pattern size, which the
  match timeout can't bound (UI-thread freeze; reachable via an imported community
  ruleset opened in the editor). `RulesetLoader` already guarded it; the App preview
  didn't. Fix: promoted the cap to **one shared `public const GlobMatcher.MaxPatternLength
  = 1000`** consumed by both sites — structurally un-driftable (the #77 guard-drift class
  the `security-review-groupweaver` skill flags). `NamingPreview.Evaluate` now rejects
  over-cap patterns as `PatternInvalid` before construction with the loader's wording.
  Loader behavior byte-identical (its cap test passes unchanged). TDD; build.ps1
  -SkipAdTests green (1260 tests, 0 failed); CI green first run.
- **test-engineer found a real nuance:** .NET's `NonBacktracking` engine has its own
  automata-node limit, so the alternation test pattern already returned `PatternInvalid`
  but with the WRONG message — the RED test pins the "maximum length" wording, proving the
  pre-construction guard was the actual gap.

## Decided

- **One shared Core constant over two mirrored literals** for the pattern-length cap
  (cross-assembly App→Core; `GlobMatcher` is the documented single-source-of-truth for
  untrusted-regex ctor knobs). No ADR — private→public const on an existing SoT type.

## Gotcha

- **Two stray untracked files appeared at the repo root mid-session** (0-byte `Program.cs`
  + a generic `dotnet new console` `GroupWeaver.csproj`, 18:42) — a stray `dotnet`
  invocation by a subagent. A root-level `.csproj` makes auto-discovering `dotnet`
  commands ambiguous; removed both and committed only explicit fix paths (never
  `git add -A` with stray artifacts present).

**Next.** No active work item. Remaining v0.2 audit items still Medium/Low (edge
legibility F6/F7, interactive legend filter F10, German-DN picker fixture).

---

# 2026-06-22 - Session 25 (large-monitor / full-screen UX pass - two slices shipped)

**Phase:** Phase 4 (post-v0.2 UX). Triggered by a `/frontend-design` review of "the UX
in full-screen mode": the workspace was a 1280x720 layout *stretched* onto big screens -
a hard-pinned 300px rail that was at once cramped and mostly empty (a tall void below
the content), no full-screen, no presentation mode, no way to find a node or reset the
camera. Shipped two vertical slices end-to-end (branch -> implementer/test-engineer ->
build.ps1 + ui-verifier/test-graph-bundle -> reviewer APPROVE -> squash PR -> ci-sentinel
green -> issue closed). `main` at f96b80a; all green on CI.

## Done

- **#114 full-screen + focus mode + adaptive rail (PR #115, ADR-022).** Native chrome:
  F11 full-screen + Esc; focus/presentation mode (`ShellViewModel.IsFocusMode` hides the
  command strip + collapses the rail -> graph edge-to-edge); the rail is now a
  GridSplitter-resizable (clamp 300-520, default 340) + collapsible (Ctrl+B) region,
  width/state persisted to `%APPDATA%\GroupWeaver\ui-state.json` (`UiStateStore` -
  RulesetLocator-shaped, never-throw, atomic). A Scope-summary card fills the no-selection
  void (object/edge + per-kind + severity tallies); the four actions reflow into a
  WrapPanel. All beside/around GraphHost - nothing over the WebView2 HWND (ADR-001).
  test-engineer caught a real demo-discipline bug: the screenshot fixtures were writing
  rail state to the real `%APPDATA%` (would pollute later frames) - fixed with a per-call
  temp-dir store. ui-verifier flagged the collapse chevron as too small (subpixel fringe
  on a font glyph) -> swapped to a vector PathIcon, re-verified clean.
- **#116 in-graph control cluster + find (PR #117, ADR-023).** Web-layer `#controls`
  overlay (sibling of the legend): Find a node by Name OR DN (selects + frames it like a
  tap - one nodeClick + applySelection + a LOCAL eased frame, NEVER emits `focused`, so
  the ADR-020 FocusCalls pin holds), Fit, Zoom +/-, and a Labels auto/all toggle (all
  labels at fit zoom, persisted across lazy-expand). Web-layer keyboard (Ctrl+F/Ctrl+0/+-)
  scoped to the bundle, no collision with the native F11/Esc/Ctrl+B. **No C# / wire change**
  - find rides the existing nodeClick path. +59 verify.mjs asserts (366 total).

## Decided

- **WP-C folded into WP-B:** the graph-control keyboard shortcuts live in the web layer
  with the controls they drive - a separate PR would have been an artificial split.
- **In-graph controls are web-layer, never native** (ADR-001): the legend already proved
  the `position:fixed` overlay pattern; `#controls` is the first `pointer-events:auto` one.

## Gotcha (cost a CI cycle)

- **verify.mjs control-driving hung the 2-core CI runner** (300s watchdog, PR #117 run 1).
  Real root cause (test-engineer bisected, reproduced 3/12 locally): a background-tap
  `page.evaluate(() => cy.emit('tap', ...))` **implicitly returned the cytoscape core
  collection**, which Playwright returnByValue-serialized over CDP - a huge cyclic object
  that wedges the renderer mid-reflow on a contended runner (the SAME trap already braced
  for the dbltap emit). Fix: brace the arrow to return undefined; drive find via
  focus+keyboard; bound the control clicks with `{timeout: MESSAGE_TIMEOUT_MS}`; add a
  `!cy.animated()` settle barrier before the typed-`-` zoom sample. 16/16 green after.

**Next.** No active work item. Optional follow-ups from this pass: an on-graph "Esc to
exit" hint in focus mode (must be web-layer, not native-over-graph); a real interactive
legend filter (F10) now that the control-overlay pattern exists. Older Medium/Low items
remain (edge legibility F6/F7, German-DN picker fixture).

---

# 2026-06-22 - Session 25 (cont.) — focus mode made reachable + demo media refreshed

Picked up the "refresh README demo media to show focus mode" follow-up — which surfaced
a real gap and turned into one more slice (#118 / PR #119, `main` at 2f24d0d, CI green).

## Done

- **#118 reachable focus mode + demo media (PR #119, ADR-022 addendum).** Refreshing the
  GIF revealed focus mode shipped **unreachable**: ADR-022 D2's "Focus button" was never
  added to the XAML and no key entered it — only the headless `WorkspaceFocus` test drove
  `ToggleFocusModeCommand`. Closed it: a `⛶ Focus` toggle in the top command strip
  (`ShellViewModel.ToggleFocusModeCommand`, visible only on the workspace step via the new
  `IsWorkspaceStep`) + a single `F` key in `MainWindow.OnKeyDown` gated to the workspace.
  Pinned by a real dispatched-key test (`KeyPressQwerty`).
- **Demo recorder + media.** Added `WM_KEYDOWN`/`WM_KEYUP` + a `Send-Key` helper to
  `webview-capture.ps1` (single-key posted to the MAIN HWND — bypasses Chrome-child focus;
  chords can't carry modifier state via PostMessage) and a focus beat to
  `record-demo-gif.ps1` (posts `F`; no F11 — that would change the capture dimensions).
  Re-recorded `m2-explore.gif` (ends on the focus-mode reveal) and refreshed the two static
  PNGs (now show WP-B's control cluster they predated). README features/alt-text updated.

## Verified

- A 5-agent media-audit workflow checked ALL 23 GIF frames + both PNGs: **zero identity
  leak** (fictional `weavedemo.example` only — no `agdlp.lab`, no operator identity, no
  connect card); focus mode visible frames 18–22.

## Gotcha (confirmed, not hit)

- Posting a single-key `WM_KEYDOWN` to the Avalonia main HWND DOES reach `Window.OnKeyDown`
  even while the WebView's `Chrome_RenderWidgetHostHWND` holds Win32 focus (PostMessage to a
  specific HWND bypasses focus routing). This is why the demo focus toggle is a single `F`,
  not the `Ctrl+B` rail chord.

**Next.** No active work item. The optional follow-ups above (web-layer "Esc to exit" hint,
interactive legend filter) and the older Medium/Low items remain.

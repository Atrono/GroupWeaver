# 2026-07-12 - Session 48 (v0.4.5 released -> both new release gates fired and held)

**Phase:** Phase 4 (feedback-driven). User approved cutting v0.4.5. The release
exercised the session-47 machinery for real: step 0 (scoped fit-audit pre-flight)
caught a NEW Bucket-A defect, and the security gate caught the unmet ADR-037 D9
redaction gate - both fixed BEFORE the tag instead of demoted. v0.4.5 is live,
public zip verified (SHA256 + provenance + launch smoke, exit 0).

## Done

- **v0.4.5 released.** Tag on `c4cd62e` (PR #341); release.yml green; public zip
  `190fd916...` matches sidecar, `gh attestation verify` exit 0, extracted exe
  banner `GroupWeaver 0.4.5+c4cd62e` + demo connect. CHANGELOG covers sessions
  41-48 (headline: exports-you-can-trust + LDAP sealing + redaction).
- **Step-0 catch / PR #339 (squash `a30e6ad`).** Gap HTML export painted Added
  rows Info-blue / Removed rows severity-Error-red (the #329 rework, post-audit
  code, NO prior pin - which is how it shipped). Two-hex fix + per-rule hue pins
  + a severity-hexes-never-in-gap-HTML negative. Worktree-isolated TDD.
- **WP10 / #249 / PR #340.** ADR-037 D9/D10: session-salted dn#/host#/path#/run#
  tokens, free-text scrubber, default-redacted every level, --log-plain with
  -PLAIN marking + warning line, crash-marker scrubbing, D9 sweep test, Settings
  Open-logs-folder, bug-template safe-to-attach line. Reviewer REJECT caught the
  marker's raw logFile path (full user-profile path) - fixed + pinned. e2e driver
  launches --verbose-logs --log-plain (demo data; justified). Smoke 10/10.
- **Media refresh** (release branch): README PNGs + GIF re-recorded for the
  "Unchecked" legend; recorder recalibrated twice, both probe-derived (pan +100px
  for the taller legend; rendered rust (136,94,69)->(129,103,91) - source palette
  pin-verified unchanged, the composited blend drifted with the rendering stack).
- Earlier same session (pre-release, journaled in 2026-07-11 second wave):
  #333/#329/#330 shipped as PRs #336-#338.

## Decided

- **Gates fire, work stops: both release-gate findings were FIXED pre-tag, not
  demoted.** The demote path exists for pre-existing non-regressions only (the
  helpsheet B-items used it, with rationale in the release PR body).
- **ADR-037 D9 satisfied** per reviewer verdict; residuals recorded (pdb paths in
  genuine local-build stacks; scrub grammar edges below the pinned corpus - both
  over-redaction-biased and noted for the future --diag work).
- **Repo auto-merge stayed off** (external settings change, session 47);
  merge-on-green via background check-then-merge works fine.

## Next

- a11y ladder: #317-#321 (names), #331 (focus order), #315/#325 (contrast retone
  pair); evidence campaign #332; help-sheet nits #326; container sweep #322.
- Consider a small follow-up: helpsheet checklist clause + keyboard/theme frames
  (release PR demotion debt, tracked in the #326/#331/#332 cluster).

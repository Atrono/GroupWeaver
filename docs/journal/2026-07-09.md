# 2026-07-09 - Session 45 continued (2) - structural risk assessment -> WP1-4 shipped

**Phase:** Phase 4 (feedback-driven). User asked whether the 2026-07-06/07 fit-audit
fixes were noted on GitHub - verified rather than assumed, then asked whether any
open topics from that session were still untracked. They were: two bucket-A
findings and the deferred backlog item, all now filed.

## Done

- Confirmed via `gh issue view`/`gh api .../timeline` that all 6 issues from the
  fit-audit thread (#268-272, #280) are closed on GitHub: #268-272 each carry an
  explicit close-comment naming their merged PR(s); #280 was auto-closed by PR #284's
  `Fixes #280` commit trailer, with the timeline showing it cross-referenced from
  both #269 (where it was found) and #284 (where it was fixed).
- **Audited the fit-audit doc's `bucketA` ("ship now") list against the 5 levers'
  `Subsumes:` lists and found a real gap**: `workspace-2` and `settings-2` were
  flagged bucket-A but not covered by any of the 5 levers that became #268-272, so
  they were never filed or fixed. Verified both bugs are still live in code
  (`WorkspaceView.axaml:132`'s hardcoded `#33808080` splitter hairline;
  `SettingsWindow.axaml:129`'s fixed-`Width="200"` glob-preview TextBox clipping its
  own watermark) and confirmed via `gh issue list` that no existing issue (open or
  closed) covers either. Filed **#285** (splitter hairline) and **#286** (glob-preview
  clipping). Also filed **#287** for the `TargetLine` DN-mono-styling backlog item
  (deferred in PR #274, previously tracked only as a note on now-closed #272 - had no
  live home).

## Decided

- **A lever's `Subsumes:` list is not automatically exhaustive over its triage
  bucket - verify it before treating a bucket-A finding as covered.** The 5-lever
  synthesis grouped 30 of 44 findings; the other 14 (2 of them bucket-A priority)
  were silently left out of every issue. Worth a spot-check like this after any
  future `fit-audit` run before considering the triage complete.

## Done (continued - later same day)

User asked for a fresh senior-architect structural risk assessment (read-only
census, knowns/unknowns), then to execute the resulting fix backlog. Assessment:
clean on every non-negotiable (no AD-write paths, perf-contract discipline
honored, DN.Comparer used everywhere) - real gaps were an N+1 LDAP pattern, a
silent auth-hardening gap, an unproven perf target, and ViewModel bloat.

- **#288 / PR #290 (squash `968f57a`).** `LdapProvider.GetMembersAsync`'s N+1
  per-member LDAP bind replaced with chunked, filter-escaped batch subtree
  searches rooted at the domain naming context (members can live outside the
  configured scope, e.g. FSPs). New `LdapFilter.Escape` (RFC 4515) is the
  injection defense for the first filter this codebase composes from
  directory-derived data. ADR-039 (Accepted). Fast-follow #289 filed for
  >200-member chunk-boundary test coverage (not blocking - injection defense was
  the security-relevant part and is fully covered).
- **#291 / PR #292 (squash `9a82407`).** Every `DirectoryEntry` bind now requires
  Kerberos sealing+signing (new `OpenEntry` factory) instead of silently
  defaulting to auth-only. A domain that can't satisfy it now fails the bind
  loudly instead of running unprotected. ADR-040 (Accepted). Live-AD suite
  (46 tests) re-verified green - the lab domain satisfies the new floor.
- **#293 / PR #294 (squash `8f5ce1b`).** New deterministic ~10K-object/~10K-edge
  in-memory synthetic-snapshot test proves `GraphBuilder`/`RuleEngine`/
  `SnapshotDiff`'s Edges-single-read perf contract at scale (previously
  convention-only). Observed ~120/290/60ms respectively, wide headroom under a
  5s ceiling. Deliberately narrower than the existing tracked #248 (WP9's CLI
  `--demo-data` seam + live-AD lab-fixture generator), which stays separate.
- **#295 / PR #296 (squash `9d26e95`).** First `AuditViewModel` (1392 -> 1349
  lines) decomposition slice: export mechanics extracted into a new
  `AuditExportService` collaborator. `[RelayCommand]`s stay on the VM
  (CommunityToolkit.Mvvm's `CanExecute` needs same-type membership); no
  AXAML/binding changes. Filtering/sorting/triage/run-history clusters are
  larger, higher-risk follow-ups, deliberately deferred (see planner's cluster
  analysis on #295).

Every WP: planner/implementer/test-engineer -> `reviewer` subagent APPROVE ->
push -> `ci-sentinel` green -> squash-merge -> issue auto-closed. Full
`build.ps1` gate green throughout, including the live-AD suite where relevant.

## Decided (continued)

- **A filter escaper is now a real thing this codebase needs, not a
  hypothetical** - ADR-039 is the first ADR admitting an LDAP filter composed
  from directory data; `LdapFilter.Escape` is the one place that happens and
  every future filter-composing change must route through it.
- **Kerberos sealing+signing over LDAPS** - works wherever Kerberos already
  works (no per-DC cert dependency), so it's now the unconditional floor, not a
  configurable option (ADR-040 D3).
- **God-class decomposition ships as reviewed slices, not one big rewrite** -
  each cluster gets its own PR/reviewer pass; stopping after one slice per
  session is fine, the backlog issue tracks what's left.

## Next

- (clear) - #285, #286, #287 (from earlier today) and #289 (chunk-boundary test
  fast-follow) are small, well-scoped, low-risk fixes for the next slice.
  `AuditViewModel` decomposition has 5 more clusters mapped (filtering/sorting
  is the big one) if continuing that thread. Standing backlog otherwise
  unchanged: WP7-WP10 (#246-249), #253, #265.

# 2026-07-11 - Session 47 (UX-gap initiative -> all 6 work packages shipped)

**Phase:** Phase 4 (feedback-driven). User asked what to change to find frontend/UX
gaps now AND continuously; a 6-WP plan (approved after two review rounds that added
loop guardrails, abort-state cleanup, font determinism, per-WP delegation) shipped
end-to-end in one session. Verdict: discovery is now self-triggering (gates + cadence
+ intake), and the machinery immediately paid out - 14 new issues of real findings.

## Done

- **WP1 / PR #314 (squash `aa6dc1b`).** Audit leftovers #285 #286 #287: splitter
  token, glob-preview stretch, Connect target line mono-DN split. ui-verifier 3/3.
- **WP2 / PRs #316 (`a07e026`) + #323 (`017298f`).** check-contrast.ps1 `-Gate` in
  build.ps1 (shrink-only allowlist; found #315 on arrival) + AccessibleNameSweepTests
  (35 real violations -> mechanism issues #317-#321, container follow-up #322) +
  three ui-checklist blind-spot sections (scaling / keyboard+names / exports).
- **WP3 / PR #328 (`adbb554`).** Targeted 29-agent fit-audit over the never-inspected
  areas, grounded in REAL export artifacts + 2x captures: 95 findings, 5 levers ->
  issues #329-#333. Headline: findings CSV breaks Excel/RFC-4180 (#329) and the
  emitted plan script aborts on FSP-bearing groups per our own lab notes (#330).
  gap-11 checklist drift ("Back to plan") fixed in the same PR. Labels ux/a11y created.
- **WP4+4b / PR #335 (`369f1cf`).** ADR-041 (Accepted): NO pixel baselines ever;
  structural gates instead - TextClippingSweepTests (embedded-font pins: Selawik +
  Cascadia Mono, canaried; Cascadia is installed NOWHERE today), verify.mjs geometry
  + deviceScaleFactor:2 probe (574->713 asserts), verify-export.mjs over a new
  `--dump-export` demo-only seam (first automated opening of a written export).
  Judge loop bounded: 3 rounds/item, progress predicate, git-restore abort.
- **WP5 / PR #324 (`c37e8f7`).** Fit-audit cadence: cut-release step 0 (A-findings
  block the tag) + wrap-session 14-day staleness nudge. Cron rejected deliberately.
- **WP6 / PR #327 (`87381fd`).** Feedback intake: ux_feedback.yml form, in-app
  "Report an issue" link (constant-base URL, encoded prefills, structural guard,
  13 tests incl. hostile-input + template drift pins), README support section.
  Debt filed honestly: #325 hyperlink dark ink, #326 help-sheet nits.

## Decided

- **ADR-041: visual regression = structural invariants + token pins + bounded LLM
  judgment; committed pixel baselines rejected as a class** (disposable-box font
  drift, self-reviewed churn, gitignored artifacts). Revisit needs a new ADR.
- **Gate allowlists ratchet shrink-only everywhere** (contrast gate, name sweep,
  clipping sweep): an entry that stops matching a live failure FAILS the gate.
- **Measurement tests must pin an embedded font** - never the ambient system font
  (ADR-041 D3; local-green/CI-red class closed before it opened).

## Next

- Levers #329 (CSV/HTML export correctness) and #330 (plan-script guards) are the
  highest-value next WPs - both are real user-facing product bugs.
- a11y debt ladder: #317-#321 (names), #331 (focus order), #315/#325 (contrast).
- Evidence campaign #332 (scaled-native captures, fixture dedup) when a session
  can afford the desktop-scale flipping.

# 2026-06-13 — Session 8 (Phase 3 start: AP 3.1 done, follow-ups cleared)

**Phase:** 3 (Regel-Engine & Release) — AP 3.1 merged (#44 → b1f8bc1, closes #37);
next AP 3.2 (#38, design workflow already running at session close).

- **Done:** Phase-3 issues #37–#41 created. Follow-ups cleared first: #28 ASCII
  wire pin (#42; review caught a false encoder claim — on .NET 8 BOTH relaxed
  encoders still escape U+2028/29, the umlaut is the real tripwire) and #29
  escape-aware DemoProvider scope via DnPath (#43; TDD, reflection seam because
  the demo dataset deliberately has no escaped DNs; LdapProvider scopes
  server-side, nothing to align). Then AP 3.1 end-to-end: judged 3-way design
  panel (admin lens won 47/43/43; all regexes + ignore globs mechanically
  verified against the 40 demo group names) → ADR-008 → 9 slices as sequential
  TDD pairs (orchestrated workflow, 15 agents) → PR #44 reviewer-approved with
  zero blocking findings → 675 tests, 0 skipped, CI green. agdlp-domain skill
  fleshed out from stub. #45 filed for the non-blocking review findings.
- **Decided:** ADR-008 — JSONC via STJ (Core stays BCL-only), schema v1 with
  fail-closed `unlisted` matrix fallback, whole-file precedence (no merge),
  NonBacktracking-only patterns/globs, `%APPDATA%\GroupWeaver\` convention.
  **#14 closed:** no dataset/lab change — DL row External cell = `info` makes the
  default ignore list demonstrably suppress exactly the 3 fixture findings;
  localized-builtin case covered by a read-only `<SID=S-1-5-32-544>` RequiresAd
  test. Binding facts → `.claude/rules/rule-model.md`.
- **Gotcha:** a stale agent worktree with deleted git metadata let the shell's
  persistent cwd silently retarget git ops at the main repo while relative
  paths resolved against the dead dir — re-`Set-Location` after worktree
  cleanup; remove worktree dirs only after the owning agent's processes exit.
- **Next:** AP 3.2 RuleEngine (#38) from the pinned baseline (3 nesting errors /
  1 cycle / 3 naming warnings / 12 empty infos / 0 External infos), fold in #45
  items 1–3. Open: #30, #45, Project board PAT scope (human:
  `gh auth refresh -s project,read:project`).

# 2026-06-13 — Session 9 (AP 3.2 RuleEngine done)

**Phase:** 3 — AP 3.2 merged (#47 → b3e6353, closes #38); next AP 3.3. #45
closed via PR #46 first (locator unreadable-arm pin, serializer cleanup-catch
widen, two endpoint:any accept pins).

- **Done:** Judged 2-way engine design panel (consumer- vs correctness-first),
  adversarially audited → ADR-009 → 9 slices as sequential TDD pairs (workflow,
  14 agents): RuleViolation/RuleReport (flat, Dns[0] anchor, Dn.Comparer
  indexes, projection-not-record equality), Evaluate (pure static, Edges read
  once, suppression pipeline), naming, nesting matrix, circular+frontier sweep
  (Walk-only, canonical min-DN rotation never reversed, cross-start dedup),
  demo baseline pinned (exactly 19 findings 3/1/3/12/0 with exact subject DNs,
  both-ways builtin suppression), live-lab integration (FSP both ways,
  partial-scope frontier proof). Reviewer APPROVE, zero blocking. 778 tests.
  Binding facts → `.claude/rules/rule-engine.md`.
- **Decided:** ADR-009 — full re-eval per expand/edit (no incrementality),
  engine-side aggregation so GraphBuilder stays a pure topology projector,
  UncheckedDns = walk frontiers ∪ load-state scan, never ignore-filtered.
- **CI gotcha:** first PR #47 run failed CS0173 — `array.Reverse()` under
  ImplicitUsings binds to LINQ on the box SDK 8.0.421 but to
  `MemoryExtensions.Reverse(Span<T>)` (void) on CI's 8.0.422; `build.ps1` was
  green locally. Fixed with unambiguous `Array.Reverse`; filed #48 to pin the
  SDK (global.json) and close the local-vs-CI drift.
- **Next:** AP 3.3 settings editor (#39 — render validation messages/notes as
  plain text per the #45 carry-over) or AP 3.4 traffic light (#40, composes onto
  the report). Open: #30, #48, Project board PAT scope.

# 2026-06-13 — Session 10 (AP 3.4 graph traffic light done)

**Phase:** 3 — AP 3.4 merged (#49 → 8f1d1ac, closes #40); next AP 3.3 (#39).
Phase 3 now: 3.1 ✅, 3.2 ✅, 3.4 ✅; remaining 3.3 (settings) + 3.5 (release).

- **Done:** Judged 2-way design panel (graph-rendering vs sidebar-UX),
  adversarially audited → ADR-010 → 6 slices (workflow, TDD pairs + two
  ui-verifier gates with bounded fix loops): severity wire fields on NodeDto +
  the one shared MapNodes; `--dump-graph` carries severity (Playwright fixture);
  cytoscape overlay halos + roll-up ring (verify.mjs tripwire); SeverityConverters
  + violations sidebar + right-column vertical split; VM wiring (Evaluate at the
  two build sites, ComputeBelow = loaded-only Walk, JumpCommand, renderer seam).
  Both ui-verifier gates PASS; reviewer APPROVE incl. the two flagged risk items.
  823 tests. Closed reviewer should-fixes pre-merge: rendered the selection
  highlight (#330F6CBD, was computed/tested but unbound) and documented the
  ADR-005 cache refinement in ADR-010.
- **Decided:** ADR-010 — severity owns the unused cytoscape `overlay-*` channel
  (never fill/shape/border); pinned palette Error #D13438 / Warning #F7A30B /
  Info #4FA3E3 + monotonic padding 7/6/5 (colourblind-redundant); GraphBuilder
  stays report-blind (severity joined in the App wire mapper); roll-up = a ring
  cue, count authoritative in the sidebar (canvas-only cytoscape has no numeric
  badge); EffectiveRuleset threaded through the composition root.
- **Refined:** ADR-005 D3 expand cache-hit guard narrowed to "no cached members"
  — a loaded-and-empty fetchable group re-fetches + re-Evaluates on expand
  (sheds its empty-group finding); null-vs-empty contract preserved.
- **ui-verifier note:** report-order sidebar put only error rows above the fold,
  so W/i glyph colours weren't statically evidenced → added a header
  severity-summary chip strip (E n · W n · i n), now the always-visible
  three-severity evidence (pinned in ShellScreenshotTests).
- **Next:** AP 3.3 settings editor (#39; render messages/notes as plain text per
  the #45 carry-over). Open: #30, #48, Project board PAT scope (human:
  `gh auth refresh -s project,read:project`).

# 2026-06-13 — Session 11 (AP 3.3 settings/rule editor done → only AP 3.5 left)

**Phase:** 3 — AP 3.3 implemented on `feat/ap-3.3-settings-editor` (ADR-011),
PR open/closing #39. Phase 3 now 3.1 ✅ 3.2 ✅ 3.4 ✅ 3.3 ✅ — only AP 3.5
(release v0.1) remains before milestone M3.

- **Done:** Judged 2-way design panel (editor-UX vs round-trip-safety),
  adversarially audited → ADR-011 → 9 slices (workflow, 22 agents; 4 logic TDD
  pairs + 4 UI slices each ui-verifier-gated): NamingPreview/GlobPreview pure
  helpers (throwaway never-interned regex); the editable ObservableObject mirror
  tree with the BYTE round-trip fixed point (`Serialize(BuildRuleset(LoadFrom(
  LoadDefault())))` == `Serialize(LoadDefault())` — sparse-matrix Present flag +
  deny/error token distinction); validation gate + import/export/reset +
  invalid-file-on-open (file byte-unchanged); NamingPreviewConverter; the modal
  SettingsWindow + TabControl (Rules/Naming/Matrix/Ignore&Exceptions/File) with
  live preview, 3×6 matrix grid, ignore/exception editors, plain-text validation
  panel (#45); shell "⚙ Settings" command + live re-thread. All 4 UI gates PASS
  (S5 after one fix); S7 evidence gap closed (endpoint control + control-char
  note + File tab now in captured PNGs). 933 tests. ui-checklist + skill updated.
- **Decided:** ADR-011 — modal SettingsWindow (ADR-003 D5 escape hatch, not a
  4th step or a GraphHost overlay); editable mirror + `RulesetLoader.Load` as the
  single save/import/apply validation gate; matrix Present-flag preserves the
  byte fixed point and the examples/ drift pin; Apply/Save re-thread via
  `WorkspaceViewModel.ApplyRulesetAsync` (re-Evaluate + UpdateGraphAsync, NEVER
  GraphBuilder.Build — pinned `Assert.Same(Graph)`); live preview never interns
  patterns; messages + notes render strictly as plain text.
- **Carry to reviewer / follow-up:** `OnRulesetApplied` is `async void` (modal
  event-handler, fire-and-forget re-thread — intended); default ruleset ships 24
  dn ignore entries (prose said 23 — descriptive, byte-pinned default is truth);
  the examples/rulesets/ "GG←GG error" sample for the M3 zip deferred to AP 3.5.
- **Next:** AP 3.5 release v0.1 (#41) — portable single-file .zip + SHA256 +
  build provenance attestations + README (demo-mode GIF, "verify your download",
  "what GroupWeaver doesn't see"); run /security-review before tagging → M3.
  Open: #30, #48, Project board PAT scope.

# 2026-06-13 — Session 12 (AP 3.5 release tooling — last package; M3 gated step pending)

**Phase:** 3 — AP 3.5 release TOOLING implemented on `feat/ap-3.5-release-tooling`
(ADR-012), PR open. Phase 3 features all done (3.1-3.4 merged); AP 3.5 tooling
is the last build step. **M3 itself** (the public v0.1 tag/release) is the
SEPARATE gated step after this PR merges + /security-review passes.

- **Done (tooling, single reviewed PR):** ADR-012 (self-contained single-file
  win-x64, NOT trimmed; web/ + examples/ loose; WebView2 a documented
  prerequisite; integrity = SHA256 + attestation, no cert); the deferred
  `gg-nesting-forbidden.jsonc` example (default + one GG←GG cell→error) + load
  pin; `tools/pack-release.ps1` (publish → web-byte-integrity → launch smoke →
  zip → sha256 → entry-manifest verify), verified end-to-end locally (89.9 MB
  exe, 12-entry zip, hash 5faf2513…); tag-triggered `release.yml`
  (attest-build-provenance@v2 + gh release create); README v0.1 flip
  (system-requirements + verify-your-download + SmartScreen note); CHANGELOG.md.
  934 tests still green.
- **Bug found by running the pack end-to-end (not just writing it):** the
  launch smoke first HUNG 44 min. Two real defects, both fixed: (1) bare
  `--check` hits the live LdapProvider — no DC on a GitHub runner → every
  release would exit 1, and the bind can block; switched to `--check --demo`
  (deterministic, no AD). (2) `Start-Process` on this WinExe DEADLOCKS when only
  one std stream is redirected — `Invoke-PublishedExe` now redirects BOTH + a
  120s kill-timeout so a hang fails fast. Lesson: a pack script's launch smoke
  must be exercised, not assumed.
- **Decided:** keep attestation action `@v2` (the design audit wrongly called
  `@v4` fabricated; live gh-api shows v4.1.0 exists but is a thin actions/attest
  wrapper upstream steers away from — v2 is the last standalone purpose-built
  major; verify is version-agnostic).
- **Next (gated M3 step, after this PR merges):** `/security-review` → resolve
  findings → bump `Directory.Build.props` 0.1.0-dev→0.1.0 → tag `v0.1` → push →
  release.yml builds+attests+publishes → download the public zip and run
  `Get-FileHash` + `gh attestation verify` → **M3**. Open: #30, #48, Project
  board PAT scope.

# 2026-06-13 — Session 13 (**M3 reached — v0.1 released. Phase 3 + the whole v0.1 plan complete.**)

**Phase:** 3 **COMPLETE**. Milestone **M3 = public release v0.1** reached.
Release: https://github.com/Atrono/GroupWeaver/releases/tag/v0.1

- **Done (the gated M3 step):** Pre-release `/security-review` ran as a 4-dimension
  adversarially-verified audit (LDAP injection / WebView bridge / untrusted
  parsing / supply chain) → verdict **CLEAR-TO-TAG**: zero critical/high; LDAP
  filter injection structurally absent (every `DirectorySearcher.Filter` is a
  compile-time constant), ADsPath bind escaped, WebView JSON ASCII-safe (#28
  tripwire holds), all 4 untrusted-regex sites NonBacktracking, no AD-write path.
  Non-blocking hardening → #52. Then: version bump 0.1.0-dev→0.1.0 (SmokeTests
  pin synced, main CI green), tag `v0.1`.
- **Release run:** first release.yml run FAILED only at the attestation step —
  *"Feature not available for user-owned private repositories."* The repo was
  PRIVATE despite CLAUDE.md/PLANNING specifying public. Surfaced the doc-vs-reality
  contradiction + the outward-facing consequence to the user; user chose to make
  it public. `gh repo edit --visibility public`, re-ran the workflow → green.
- **M3 DoD verified against the PUBLIC artifact:** downloaded
  `GroupWeaver-0.1.0-win-x64.zip` (38.3 MB) + `.sha256`. SHA256 matches the
  sidecar; `gh attestation verify … --repo Atrono/GroupWeaver` → exit 0,
  provenance = built by `release.yml@refs/tags/v0.1` on a github-hosted runner;
  extracted the zip and ran the published exe (`--check --demo`) → exit 0,
  `GroupWeaver 0.1.0+4924bb6 … connected, 40 groups loaded`. The portable build
  launches, is hash-verifiable, and is provenance-attested.
- **The pack tooling held up in real CI:** publish + launch smoke + zip on the
  GitHub runner all passed (the local launch-smoke hardening from session 12 —
  `--check --demo` + both-stream redirect + timeout — was load-bearing).
- **Decided:** repo is now public (matches the project's open-source intent);
  keep attestation action `@v2`.
- **State:** Phases 0–3 done, M0–M3 reached. v0.1 (Ist-Modus + convention traffic
  light) is publicly released and verifiable. Open follow-ups for v0.1.x: #30,
  #48, #52, and the Project-board PAT scope. Backlog (PLANNING §7): plan mode,
  gap analysis, Entra ID, exports — feedback-driven, post-v0.1.

# 2026-06-13 — Session 14 (post-v0.1 security hardening — #48 + #52 closed)

**Phase:** post-release. All v0.1 security-review follow-ups cleared (#53 →
2891b95, closes #48 + #52); main CI green on the first run with the new
toolchain pin. Next: #30 (last Phase-2 follow-up, design running), then Phase 4.

- **Done (one reviewed PR #53):** the pre-release review's non-blocking items.
  Supply chain — verified the vendored `cytoscape.min.js` is byte-identical to
  upstream npm `cytoscape@3.34.0` (SHA `9c2a3bf2…`) and found a latent bug en
  route: `* text=auto` was rewriting the minified bundle to CRLF on Windows
  checkout, diverging on-disk bytes from upstream and making WebBundleTests
  self-referential. Marked `src/App/web/vendor/** -text`, recorded the upstream
  SHA + source in THIRD-PARTY-NOTICES, repinned the test to the upstream hash.
  All workflow actions pinned to commit SHAs. Toolchain — `global.json` pins SDK
  8.0.421 / rollForward latestPatch, both workflows use `global-json-file`.
  Defense-in-depth — CoreWebView2 hardened (DevTools off, new-window suppressed,
  navigation locked to `file://` — made scheme-based after the reviewer caught
  that exact-URI matching could blank the graph), GlobMatcher.Cache bounded
  (4096, clear-on-cap), finite 2s regex MatchTimeout at all four untrusted sites.
  945 tests; reviewer approved (1 should-fix on the nav guard → fixed → re-approved).
- **Decided:** keep attestation `@v2` (v4 is a thin actions/attest wrapper);
  graph-layer / scheme-based hardening over snapshot/contract changes.
- **Gotcha → kept in mind:** vendored binaries need an explicit `-text` in
  `.gitattributes`, or `* text=auto` mangles them on Windows checkout and breaks
  any upstream-SHA integrity check.
- **Next:** #30 reload+prune (design running), then Phase 4 v0.2 (export first).

# 2026-06-13 — Session 15 (#30 reload + Phase 4 begins: AP 4.1 export shipped)

**Phase:** post-v0.1 follow-ups cleared + Phase 4 (v0.2) started. #30 merged
(#55 → 3da9917); AP 4.1 export merged in two halves (#57 report → 34e1935,
#58 graph image → 52e14fe, closes #56). Only #54 (graph-layer pruning, deferred,
needs its own ADR) remains open.

- **#30 whole-scope reload (no ADR — ADR-005 addendum):** the adversarial design
  found reload eliminates orphan ex-member nodes BY CONSTRUCTION (a fresh
  `LoadScopeAsync` rebuilds the snapshot; `GraphBuilder` is total over Objects),
  so it ships ZERO pruning code. `ReloadScopeCommand` → fresh snapshot →
  `ShowGraphAsync` (replace-all, viewport reset honest); shares `RunScopeLoadAsync`
  with the ctor load; selection-independent gate. Snapshot stays append-only
  (pinned in data-model.md — no `RemoveObject`). Graph-layer pruning deferred → #54.
- **AP 4.1 export (ADR-013, v0.2's first feature):** report-first, PNG-only
  (SVG deferred — cy.png is on the vendored cytoscape core, no new dep).
  - Report half: `ViolationReportExporter` (Core, App-free, `ResolveName`
    delegate) → CSV (formula-injection guard BEFORE RFC-4180 quoting) + a
    self-contained HTML report (every untrusted token `WebUtility.HtmlEncode`d,
    tokens only in text nodes — no CSV-injection, no XSS, #45); VM commands gate
    on `Snapshot is not null` (not HasViolations — the unexpanded-areas appendix
    is exportable in the all-clear state); sidebar Export CSV/HTML buttons.
  - Graph half: `cy.png()` bridge round-trip — `exportPng`/`pngExported` wire
    pair, `IGraphRenderer.ExportPngAsync` mirroring `FocusAsync` (single-flight,
    60s, null-on-timeout); F1 honored (valid → `PngExportedMessage`, never
    `UnknownMessage`→`RendererError`); Playwright PNG-magic-byte gate before the
    zero-jsError audit; "Export image" button. Read-only throughout (writes only
    the user-picked path; no AD touched).
  - Two ui-verifier PASSes, two reviewer APPROVEs, Playwright + 1023 tests green.
- **Next (Phase 4 v0.2 remaining):** Plan mode (drag&drop node design, live
  validation, PowerShell-script export — stays read-only) is the next big
  feature; winget (O5) needs a Win-11 env (not available here) → deferred.
  Open: #54.

# 2026-06-13 — Session 16 (**Plan mode (AP 4.2) shipped end-to-end — #59 + #63 closed; v0.2 headline complete**)

**Phase:** Phase 4 (v0.2). Plan mode delivered as a 4-slice arc behind ADR-014,
plus a latent export-wiring bug fix and a security hardening. #59 (plan mode) and
#63 (export seam) both CLOSED. Only #54 (graph-layer pruning, own ADR) remains open.

- **ADR-014 (plan mode):** a separate mutable `PlanModel` (NOT an editable snapshot —
  the snapshot is append-only); reuse the UNCHANGED engine/builder via
  `PlanProjection.ToSnapshot`; Plan is a sibling shell step returning to the SAME
  never-disposed workspace; a panel-based editor with the read-only graph as live
  preview (canvas editing is un-injectable in this headless box); inert PowerShell
  export. Cycles + self-membership are AUTHORABLE (findings, not errors).
- **AP 4.2.1 (#60):** Core/Plan — `PlanModel`/`PlanObject`/`PlanProjection`/
  `PlanScriptExporter` (pure inert-string builder, single-quote `'`→`''` containment,
  control-char reject). **4.2.2 (#61):** Plan reachable but empty — `PlanViewModel`
  sibling step, Ist↔Plan switch keystone (`Assert.Same` workspace round-trip), dispose
  discipline (teardown disposes both steps, the switch disposes neither). **4.2.3 (#62):**
  the editor — add object/user, add/remove membership, rename, remove, selection sync,
  live validation; `PlanNodeRowModel`/`PlanEdgeRowModel`; kind combo friendly labels
  (`PlanCreatableKindConverters`, reuse `AdObjectKindConverters` palette). **4.2.4 (#64):**
  "Export script" button → inert `.ps1` (clock+version injectable seams, UTF-8 no BOM).
- **#63 (fixed in 4.2.4):** the AP 4.1 `IExportFileDialogs` seam was unit-tested behind
  a fake but NEVER wired in production (no `StorageProviderExportFileDialogs` ctor, no
  `UseExportFileDialogs` call) → the Export CSV/HTML/Image buttons were inert in the
  shipped app. `MainWindow.OnOpened` now wires it to the current step and re-wires on
  every `CurrentStep` change (workspace AND plan), unsubscribed once on close.
  Regression test (the missing pin): opening MainWindow ARMS the export commands.
- **/security-review (pre-tag):** VERDICT PASS — no exploitable injection (hostile
  tokens stay inside single-quoted literals; control chars rejected). One LOW finding:
  `ToolVersion` bypassed the gate → hardened (#65): shared `Guard()`, ToolVersion gated
  (unquoted, it's a comment). Every emitted token now passes the gate.
- **Decided:** SVG export stays deferred (ADR-013 — cy.png covers v0.2; no new vendor
  dep); winget still deferred (no Win-11 env). With export + plan mode done, **v0.2's
  defined scope is feature-complete** (no formal v0.2 release milestone in PLANNING.md —
  Phase 4 is feedback-driven; not cutting a public release unprompted).
- **Next:** v0.3 Gap analysis (diff Ist-graph vs Plan-graph) — grounding done (reuse
  matrix + 5 crux questions: DN matching across Ist/Plan scopes, where diff-status lives
  in the graph model, sibling-step vs overlay, load-state semantics, finding synthesis).
  ADR-015 is the next deliverable. Open: #54.

# 2026-06-13 — Session 17 (v0.3 Gap analysis: ADR-015 designed + pure-Core diff foundation shipped, slices 1–3)

**Phase:** Phase 4 (v0.3, #66). ADR-015 designed and merged; the pure-Core diff layer
(SnapshotDiff + union + GapReport, slices 1–3 of 9) merged. Next: the wire + UI half
(slices 4–9). Open: #54.

- **ADR-015 (judged 3-way design panel → adversarial synthesis, reviewer-approved):**
  a pure-Core `SnapshotDiff.Compute(ist, plan)` (plan via `PlanProjection.ToSnapshot`);
  render the UNION snapshot through the UNCHANGED `GraphBuilder`; paint diff status only
  via new `WhenWritingNull` wire fields at the single `GraphJson.MapNodes/MapEdges` choke
  point (GraphBuilder stays diff-blind, exactly the ADR-010 severity pattern); a sibling
  `GapViewModel` reached from Plan mode, borrowing the Ist snapshot read-only (ADR-014
  dispose discipline preserved); exact-DN matching gated on `BaseOuDn == RootDn`; honest
  `Unchecked` for unexpanded Ist areas; a synthesized `GapReport` separate from the pinned
  `RuleReport`. The only new Core files are `src/Core/Diff/`. Deferred to named follow-ups:
  `DiffStatus.Modified`, cross-scope re-base alignment, forced pre-diff reload, merging
  gap findings into the RuleReport.
- **Slice 1 (#68) SnapshotDiff.Compute:** node + per-EXACT-edge diff (Common/Added/Removed)
  with the Unchecked keystone — an edge under a KNOWN-but-unloaded Ist parent is Unchecked
  (never falsely Added/Removed) and the parent enters `UncheckedParents`; a plan-only or
  loaded-empty parent stays Added. `Edges` read once (review-enforced; `.claude/rules/
  gap-diff.md`). **Slice 2 (#69) BuildUnion + GapSummary:** Ist-wins on Common DNs (the real
  loaded object's whitelisted Attributes survive), per-parent member union, never reads
  `.Edges`; GraphBuilder terminates over the union on an A↔B cycle. **Slice 3 (#70)
  GapReport:** Added/Removed/Unverifiable findings, edge anchor = parent, deterministic
  block order mirroring `RuleViolationComparer`, separate from `RuleReport`.
- **Process note:** slice 3 surfaced a real pre-existing test-pattern bug (a
  `(GapKind, IReadOnlyList<string>)` tuple compared inner Lists by reference → the
  determinism test could never have caught a regression) — fixed via the sibling
  `string.Join` projection idiom; and the `Assert.Empty(...Where(...))`/`Assert.Single(...
  Where(...))` forms must be the xUnit2029/2031-preferred `DoesNotContain`/filtering-`Single`
  under `TreatWarningsAsErrors`. The implementer correctly refused to weaken the lint gate.
- **Next:** slice 4 (wire — `NodeDto`/`EdgeDto.Diff` + `DiffWire.ToToken` at the
  `GraphJson` choke point, null ⇒ byte-identical), then 5 (graph.js diff channels +
  verify.mjs), 6 (renderer seam `ShowDiffGraphAsync`), 7 (GapViewModel), 8 (shell
  `OnGapAnalysis` wiring), 9 (GapView + ui-checklist). Open: #54.

# 2026-07-10 - Session 46 (harness audit -> report delivered, execution pending)

**Phase:** Phase 4 (feedback-driven). User asked for a comprehensive, read-only
review of the project's harness (init/env/dependency mgmt, `.claude/` agentic
readiness, CI/CD automation gaps), explicitly gated: report first, execute
nothing without a separate confirmation.

## Done

- Three parallel Explore agents covered `.claude/` config (agents/hooks/rules/
  skills), build/CI/dependency management, and testing structure/ADR trail; a
  Plan agent synthesized their findings into a 3-tier action plan. Verdict: the
  harness is fundamentally sound (single local/CI gate via `tools/build.ps1`,
  SHA-pinned Actions, tightly-scoped subagents, 40 contiguous ADRs) with 17
  concrete, mostly config-only gaps - no NuGet lock files/Central Package
  Management, no .NET analyzers, no JS lint gate on `src/App/web/*.js`, no
  Dependabot, no coverage measurement, plus a handful of dead artifacts
  (`tools/smoke-back-nav.ps1`, stale `.claude/settings.local.json` entries,
  `.claude/scheduled_tasks.lock` only excluded locally).
- User asked three follow-up deep-dive directives (execution-context/security,
  whether `tools/` should be a frozen agent-write-forbidden API, Biome vs.
  ESLint for the JS gate) - each verified directly against live files rather
  than re-derived from the agents' summaries. Findings: execution context is
  already clean (no hardcoded creds, no hidden elevation in the build/test
  loop); a blanket "freeze `tools/`" rule was recommended **against** - it
  would contradict CLAUDE.md's own bootstrap-currency mandate - with a
  narrower PR-flagging alternative offered instead (plan item 14); ESLint/
  Prettier via the existing `tests/graph-bundle` Node footprint beats adding
  Biome as a new tool category at this repo's ~85KB JS scale.
- Delivered the full report + tiered plan as an Artifact
  (`harness-audit-report.html`, published) and captured the same content in
  the plan-mode plan file for the session record. **Zero files modified** -
  this was entirely read-only per the user's explicit ask.

## Decided

- **Execution scope for this session was report-only** - the user confirmed
  no changes should land without a separate later go-ahead.
- **If/when Tier 2 is greenlit, include items 7 (.NET analyzers) and 9
  (Dependabot) by default** rather than re-asking - the user already opted in
  to both despite their flagged real costs (one-time analyzer triage effort;
  recurring Dependabot PR-review load).
- **`tools/` stays agent-editable, not frozen** - CLAUDE.md already mandates
  `bootstrap.ps1` be kept current with every installed dependency, and
  `build.ps1` is the repo's sole extension point for new quality gates; a
  blanket write-freeze would conflict with both. A narrower "flag gate-surface
  diffs in the PR body" rule was proposed instead, held as a Tier 3 item
  pending buy-in, not applied.

## Done (continued - execution greenlit, all 9 items shipped)

User gave the go-ahead: Tier 1 in full + Tier 2 items 6-9, strict per-item
loop (implement -> verify against the item's success criterion -> full
`build.ps1` green -> next). **#301 / PR #302 (squash `6fd26be`)** - zero
items blocked or skipped; reviewer APPROVE; PR CI + post-merge main CI green.

- **1 NuGet lock files:** `RestorePackagesWithLockFile` + central
  `RuntimeIdentifiers=win-x64` (locks stable across plain/RID/Release-eval
  restores), `--locked-mode` in the gate, CI cache key includes locks.
  Negative-tested: stale lock -> NU1004, exit 1.
- **2 CPM (tests-scoped):** `tests/Directory.Packages.props`; the lockstep
  comment is now compiler-enforced (NU1008 negative-tested).
- **3** stale `settings.local.json` allow-rules pruned (local-only).
- **4** deprecated `tools/smoke-back-nav.ps1` deleted, live docs repointed.
- **5** `.claude/` runtime-state ignores moved into the versioned `.gitignore`.
- **6 Coverage:** `build.ps1 -Coverage` (built-in collector, cobertura) +
  `tools/coverage-report.ps1` (ReportGenerator local tool, product assemblies
  only). Baseline: **90.2% line / 85.6% branch**. Default invocation unchanged.
- **7 Analyzers:** `AnalysisLevel=latest-recommended` fully enforced under the
  existing warnings-as-errors; every `.editorconfig` opt-out justified;
  CA1001/CA1068 kept live with site-targeted suppressions (reviewer feedback).
  Product fixes: culture-stable export snippets (CA1305 - German box!),
  one CA1826 indexing fix. Enforcement negative-tested (CA1305 fires as error).
- **8 Web lint:** repo-root flat `eslint.config.js` (correctness rules,
  dependency-free, vendored cytoscape excluded), eslint exact-pinned into the
  existing `tests/graph-bundle` npm footprint, check-only `tools/lint-web.ps1`
  (no --fix path exists), new CI step. Bundle bytes untouched -> publish SHA256
  check unaffected. Prettier deliberately dropped (would mass-reformat the
  hand-authored bundle - zero-source-churn criterion wins).
- **9 Dependabot:** weekly, PR-only, Avalonia* ignored (exact-pin culture);
  all three ecosystems scanned immediately on merge (config provably parsed).

## Decided (continued)

- **Lock files must be configuration-independent to survive locked-mode:** the
  Debug-only Avalonia.Diagnostics ref made `packages.lock.json` flip-flop under
  Release-evaluated restores; fixed with an `ExcludeAssets="all"` twin ref for
  non-Debug configs (verified: byte-stable locks, no dll in publish output). A
  self-contained publish still rewrites locks (implicit ILLink.Tasks) -
  `pack-release.ps1` re-normalizes afterwards; quirk documented in
  Directory.Build.props.
- **Prettier rejected, ESLint-only for the web bundle** - a formatter would
  rewrite the deliberately hand-authored bundle and fight the byte-identical
  publish gate; the lint gate is correctness-only, no mutation path.

## Next

- Tier 3 items (10-14) remain open judgment calls: bootstrap SDK-check
  tightening, CLAUDE.md pruning (now MORE pressing - new gates deserve a
  mention and the file sits at its 150-line budget), coverage threshold gate
  (baseline now exists: 90.2%/85.6%), gate-surface PR-flagging rule.
  Standing backlog unchanged: #285-287, #289, WP7-WP10 (#246-249), #253, #265.

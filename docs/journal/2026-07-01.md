# 2026-07-01 - Session 40 (open-issue triage -> #196/#197/#198 + #214 shipped; v0.4.3 released)

**Phase:** Phase 4 (feedback-driven). Triaged the 5 open GitHub issues: one was already
done, one is deferred-by-design, three were actionable UX parity/honesty gaps from the
June UX fit-audit. Shipped all three end-to-end through the subagent pipeline
(implementer -> test-engineer -> build -> ui-verifier -> reviewer -> ci-sentinel), each
branch -> PR -> green CI -> squash-merge.

## Done

- **#195 closed (no code).** Verified the Gap CSV/HTML export it asks for already shipped
  in WP #202 (GapView Export buttons -> `GapReportExporter`, guard-before-quote + RFC-4180
  CSV, HTML honesty rows, pinned by tests). Stale-open -> closed referencing #202.
- **#196 / PR #212 (squash `afa89ae`).** Root Picker disambiguates same-named OUs: new
  `DnConverters.ToParentPath` shows each candidate's PARENT path (split on the first
  unescaped comma via backslash-run parity) instead of the right-trimmed full DN, + a
  full-DN tooltip. Display-only. Gate green (970 App + 858 Core), reviewer APPROVE,
  ui-verifier PASS (3 same-named `Sales` OUs distinguishable), CI green.
- **#197 / PR #213 (squash `0904da7`).** The violations-sidebar severity chips are now
  interactive session-only filters, mirroring the Audit screen
  (`ToggleFilter`/`AuditFilterChip`/`_severityFilter`): fail-open, OR-within-axis, counts
  over the unfiltered set, header shows the unfiltered total, WCAG-1.4.1 non-hue active
  affordance (~10.9:1 dark / ~11.8:1 light). Gate green (976 App + 858 Core), APPROVE,
  PASS, CI green. `AuditView.axaml` left untouched (local styles) to keep its screenshot pins.
- **#198 / PR #215 (squash `7f7f95e`).** Each sidebar finding row gets a quiet "Why?"
  flyout surfacing the per-rule-class "Why it matters" + numbered "How to fix" copy on the
  explore->drill path, routed through `AuditFindingDetail.From` so it can never drift from
  the Audit screen. ADR-007 whitelist detail panel untouched (rationale lives on the
  sidebar). Gate green (980 App + 858 Core), APPROVE, PASS, CI green.
- **#214 / PR #216 (squash).** Follow-up cleanup filed + shipped same session: removed the
  now-unused `SeverityConverters.CountForSeverity`/`HasSeverity` (+ their private
  `SeverityCountConverter` backer + two dead usings), dead since #197 re-wired the sidebar
  chips. Pure deletion, no behavior change. Gate green (980 App + 858 Core), reviewer
  APPROVE, CI green.
- **Released v0.4.3 (tag `v0.4.3` on bump commit `cbcc3d2`, PR #217).** Bundles the
  Phase-4 feedback work since v0.4.2 (#186, #188-#211, #196/#197/#198, #214). Pre-flight
  green (build gate + `pack-release`); a 6-surface security review of the full
  `v0.4.2..HEAD` delta (incl. the new #189 connection-targeting surface) found **0
  issues**; `release.yml` published the zip + `.sha256` with provenance; public artifact
  verified (SHA256 matches sidecar, `gh attestation verify` OK, extracted exe
  `--check --demo` prints `GroupWeaver 0.4.3` + demo connection).
- **Refreshed README demo media + added a release media-currency check (PR #218).**
  Re-recorded `m2-explore.gif` (now shows the #200 detail-panel Copy button; narrative
  intact) and regenerated the two graph PNGs (they gained the #211 "double-click to expand"
  legend hint that postdated the 2026-06-25 renders; `verify.mjs` tripwires green). New
  `tools/check-media-currency.ps1` (git-timestamp heuristic, non-blocking) wired into
  `pack-release.ps1` pre-flight + a `cut-release` step, so future releases surface stale
  media instead of shipping it silently. Reviewer APPROVE, CI green. No v0.4.3 re-tag
  (media/docs aren't in the release zip).
- **New `.claude/workflows/fit-audit.js` (commit `6770fd7`, direct to main) - reusable
  multi-agent UX fit-audit** (later same day, feedback-driven tooling). Encodes the 20+
  agent topology that sessions 21/29/37/39 rebuilt by hand: one critic per app surface ->
  two diverse-lens jurors (consumer persona + WCAG/ADR correctness) triage each surface ->
  completeness critic -> synthesizer ranks Top-N levers + A/B/C triage. Read-only/advisory:
  returns structured findings only; the caller persists the doc and files issues (an audit
  never mutates the product). Surfaces/personas/topLevers are `args`-overridable; the rubric
  stays in `docs/ui-checklist.md` and settled calls in `docs/adr/` (linked, not copied).
  Validated end-to-end (full 9-surface run = 63 findings, session-37 caliber; scoped
  1-surface run = 5 agents, correct contract shape, read-only + ADR-settled handling
  confirmed); reviewer APPROVE twice. Two test-surfaced fixes: normalise `args` that arrive
  JSON-stringified (the scoping knobs were silently no-op into the full-audit default), and
  wrap the two directly-awaited terminal agents in try/catch so one StructuredOutput
  retry-cap flake degrades gracefully instead of killing the whole ~2.8M-token run.
- **Ran `fit-audit` on the full app + shipped Lever 1 end-to-end** (dogfooded the new
  workflow). Full 9-surface run (`docs/ux-fit-audit-2026-07-01.md`, commit `2530a44`): 59
  findings -> verdict + Top-5 levers + A/B/C triage (13/23/24); read-only-honest (every fix
  presentation-only) and it correctly left ADR-034 reduced-motion settled. **Lever 1 (#219 /
  PR #220, squash `e5cf306`):** glyph-only native controls (theme toggle, focus-mode, help
  `?`, rail chevron) had a tooltip but no `AutomationProperties.Name`, so a screen reader
  announced the bare glyph (WCAG 4.1.2); added state-accurate accessible names + an F1
  KeyBinding to the existing help command (SC 2.1.1/2.4.4). Presentation-only (+19 src lines).
  Gate green (986 App + 858 Core), reviewer APPROVE, ui-verifier PASS (renders SHA-256
  byte-identical - zero visual diff), CI green. Pipeline flushed one hygiene reject (a stray
  ui-verifier temp fixture + a pre-existing scratch PNG - both kept out of the committed diff).

## Decided

- **The three remaining fit-audit cheap-wins are correctness/parity, not polish.** A
  dead severity chip, an ambiguous root picker, and rule rationale reachable only from the
  Audit screen are trust/usability gaps; closing them brings the explore path to parity
  with the Audit screen. None warranted an ADR (reuse of existing patterns + copy on
  existing surfaces; issue + commit suffice).
- **Took the reviewer's DN backslash-run-parity nice-to-have inline** (not deferred) -
  cheap and correct for a reusable converter, so its doc contract stays literally true.
- **Media-currency is a NON-BLOCKING git-timestamp reminder, not a pixel gate.** Rendering
  is non-deterministic across GPU/software modes and a source change doesn't always alter the
  media, so an exact-hash CI gate would be flaky/false-positive. The check warns (exit 0;
  `-Strict` opt-in for future CI) so a human judges. Known limitation: it's `docs/media`-commit
  granularity, so refreshing any one asset marks all current - acceptable for a reminder; a
  per-asset-group refinement is the future option if it ever masks a real gap.
- **The reusable audit topology is a Workflow, not a skill - and audits stay read-only.** A
  prose skill would mostly repackage always-loaded CLAUDE.md (modest gain); encoding the
  agent fan-out as a Workflow removes the per-audit re-derivation (net-new). The workflow
  writes nothing and files no issues - the orchestrator persists results - so the
  read-only-product invariant holds even in the tooling. `args` cross the runtime boundary
  JSON-stringified, and directly-awaited terminal agents sit outside `pipeline`/`parallel`
  isolation, so they must be defended explicitly (parse + try/catch) - both caught by
  testing the workflow before trusting it.

## Next

- **Levers 2-5 from `docs/ux-fit-audit-2026-07-01.md` remain** (Lever 1 shipped): action-
  hierarchy on secondary surfaces (Root Picker/Plan/Settings File), the three unjudged graph
  overlays (command palette/minimap/issues-filter - needs a Proposed ADR + checklist rows),
  off-brand Fluent-blue selection accents + sub-3:1 kind badges, and caption-opacity
  meaning-leak on the Audit ring/tiles. Bucket-A has ~12 findings still open.
- `#54` (graph-layer pruning) stays deferred by design (ADR-016 Proposed; Reload cures it).
- Tooling: `Workflow({ name: "fit-audit" })` (a fresh session loads the committed file; within
  a session use `scriptPath` to run edits). Skill ideas not taken: `ship-work-package`,
  `triage-and-plan`.

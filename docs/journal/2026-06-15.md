# 2026-06-15 — Session 19 (v0.2.0 release-readiness pass — merged, public tag HELD for user)

**Phase:** Phase 4. Decided (with the user) to spend this session making the
accumulated-but-unreleased v0.2 (export, plan mode) + v0.3 (gap analysis) work
release-ready, rather than building more on an unreleased base. Prep is merged
(PR #76 → ffc791d); the public release TAG is deliberately held for the user's
explicit go (outward-facing). Open: #54, #77, #78.

- **Orientation correction:** the SessionStart hook showed a stale journal
  (Session 8); real state is Sessions 15–18 = v0.2/v0.3 feature-complete on main,
  only v0.1 publicly released. A 2-version release backlog was starving the
  "feedback-driven" Phase 4 — so release-readiness was the highest-leverage move.
- **Security (the headline):** ran a pre-release adversarial audit as a 5-dimension
  Workflow (plan-export injection / export CSV+HTML / untrusted parsing+regex-DoS /
  read-only invariant / WebView bridge), each finding refuted before counting.
  Read-only invariant CONFIRMED intact (no AD-write path anywhere). Found + fixed
  (TDD, PR #76): **HIGH** — `PlanScriptExporter.Guard` rejected only ASCII control
  chars, but PowerShell's tokenizer honors the Unicode quote block U+2018–U+201F as
  string delimiters, so a curly apostrophe in an authored name escaped the
  single-quoted `.ps1` literal → code exec in the operator's session (verified e2e in
  pwsh 7 + PS 5.1). Guard widened to `char.IsControl` + U+2028/U+2029 + U+2018–U+201F.
  **MEDIUM** — NonBacktracking Regex ctor unbounded (match-timeout doesn't cover it);
  capped pattern length (1000) in `RulesetLoader.ValidatePattern`. **LOW** — guarded
  the PNG-bridge base64 decode (was an async-void crash).
- **UX (holistic cross-mode review, Workflow):** verdict minor-fixes, 0 blockers.
  One real fix — flipped the Avalonia shell `RequestedThemeVariant` Default(Light)→**Dark**
  to match the hard-dark graph WebView (#1b1f27); views are theme-agnostic (alpha
  accents), re-rendered 34 screenshots, verified clean. The "Gap drops the Settings
  strip" finding was a SCREENSHOT-FIXTURE artifact (the fixture overwrites
  `MainWindow.Content`), NOT a product bug — verified, no change. Deleted stale
  gitignored diff-crops.
- **Docs/media:** PLANNING (header + Phase 4 table), README (Status + 0.2.0 download
  examples + dark stills), CHANGELOG ([0.2.0]) reconciled. README hero swapped to dark
  stills (graph + gap-diff) since the theme flip stale-d the light GIF (#78).
- **Decided:** working release version **0.2.0** (next minor; bundles export+plan+gap);
  0.2.0-vs-0.3.0 + the actual tag is the user's gated call. `Directory.Build.props`
  bump + SmokeTests pin + tag are the deferred gated step (NOT done this session).
- **Gate:** reviewer APPROVE (0 blocking), full build green (1242 tests, 0 skipped),
  CI green on #76. Filed follow-ups: #77 (author-time char parity), #78 (dark GIF).
- **Next (gated, user go):** pick 0.2.0 vs 0.3.0 → bump `Directory.Build.props` +
  SmokeTests pin → `/security-review` is already satisfied by this session's audit →
  tag `v0.2`/`v0.3` → release.yml builds+attests+publishes → verify the public zip.
- **RELEASED (same session):** user chose v0.2.0 → bumped version (70578ef, CI green)
  → tagged `v0.2` → release.yml shipped (attestation PASSED — the public-repo fix from
  session 13 held). Public artifact VERIFIED: SHA256 matches the sidecar,
  `gh attestation verify` passes, and the published exe `--check --demo` exits 0
  reporting `GroupWeaver 0.2.0+70578ef` + 40 demo groups. **v0.2.0 is publicly
  released:** https://github.com/Atrono/GroupWeaver/releases/tag/v0.2 . Filed #79
  (Node 20→24 attest-action deprecation; GitHub flip 2026-06-16). Open: #54, #77, #78, #79.

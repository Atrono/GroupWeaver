# 2026-07-06 - Session 43 (WP6b smoke-tag promotion)

**Phase:** Phase 4 (feedback-driven). User asked for a "full test for the new tests"
after last session's WP6b e2e build-out; clarified into promoting the 5 newly-shipped
scenarios from `full`-only to the fast pre-push `smoke` tag, given each already had
solid multi-run verification evidence from shipping.

## Done

- **PR #267 (squash `c0e86b2`).** Promoted `audit-zero-drift`, `settings-rethread`,
  `lazy-expand-race`, `keyboard-sweep`, `golden-journey` from `Tags=@('full')` to
  `Tags=@('smoke', 'full')` in `scenarios.psd1`. Pure manifest change, no scenario
  logic touched. `-Tag smoke` now runs all 10 scenarios in ~147s, comfortably inside
  the ~3-4 min pre-push budget (ADR-038 D7). Build gate green (813 Core + 1161 App),
  reviewer APPROVE, CI green.

## Decided

- **Smoke promotion bar: solid same-session verification is enough, since smoke is
  advisory/on-demand only** (the `e2e-run` skill before a push), not a CI gate and
  not the nightly `-Tag full` autonomy-loop safety net (WP8/#247, still open) - a
  false positive here costs developer friction, not a masked regression. Revisit
  once WP8's nightly sweep has run these 5 for a few days; demote back to
  `full`-only if any show intermittent `INFRA-DRIVE`/`TIMEOUT` signatures.

## Next

- WP7 (#246, fault pack), WP8 (#247, autonomy loop), WP9 (#248, scale + lab fixture
  additions), WP10 (#249, redaction - gates the next tagged release). Also open:
  #253 (WebView2 profile isolation no-op) and #265 (DemoConnected/composition-root
  ordering - E2eChannel subscribes after the demo auto-connect already fired).

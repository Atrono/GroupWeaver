# 2026-06-30 - Session 39 (whole-journey UX audit -> roadmap COMPLETE; 14 WPs #199-#211 shipped)

**Phase:** Phase 4 (feedback-driven). Ran a multi-agent whole-journey frontend UX audit
(Connect -> ... -> Settings + graph layer) via a Workflow; turned the findings into a two-slice
quick-wins batch + a roadmap of ADR-sized bigger bets, then shipped both slices through the
subagent pipeline (implementer -> test-engineer -> build -> ui-verifier -> reviewer -> ci-sentinel),
each branch -> PR -> green CI -> squash-merge.

## Done

- **PR #199 (squash `4abef77`) - Slice A: theme parity, vocabulary, keyboard, gap fix.**
  Headline: the four hardcoded `#C42B1C` inline-error fills -> `ValidationErrorTextBrush`
  (restores light-theme WCAG AA on the first screens - a regression of our own bar). Plus
  separator literals -> `CardBorderBrush`, "findings" vocabulary, Gap unchecked tally counts
  PARENTS not edges, Enter/double-click entry flow, "Refresh" -> "Refresh node", type-scale
  classes, placeholder rewording. 834 App + 820 Core, reviewer APPROVE, ui-verifier all PASS.
- **PR #200 (squash `d47da9f`) - Slice B: detail DN copy, settings feedback, audit headline.**
  One-click Copy + `sAMAccountName` caption in the detail panel; Apply/Save success line in
  Settings; Audit band verdict promoted to the card headline (ADR-030 intent; no tint, hue stays
  on the ring). ui-verifier caught a DN-wrap regression (horizontal StackPanel made TextWrapping
  inert) - fixed pre-merge. +15 tests, reviewer APPROVE, CI green.
- **PR #201 (squash `cfe852c`) - ADR-033: keyboard-focus-visible ring.** The one net-new WCAG
  2.4.7/2.4.11 gap (native chrome showed no visible keyboard focus). App-wide custom accent
  `FocusAdorner` under `:focus-visible` REPLACING Fluent's default (which composites in the
  headless capture and dominated white/black/blue - a masked-BoxShadow first attempt was caught
  by ui-verifier and reworked). Edge-hugging `Margin=0` (ClipToBounds), ComboBox blue suppressed,
  chip ring rounds. 15 tests incl. a render-PIXEL masking guard (the seam the BoxShadow tests missed).
- **PR #202 (squash `b014f9a`) - Gap diff export (CSV/HTML).** The only findings surface with no
  export now has one: new Core `GapReportExporter` mirroring the violation-export escaping
  (formula-guard before RFC-4180; HtmlEncode element-text-only; caller-injected timestamp), sidebar
  buttons, save-picker + VM seam. 49 tests; reviewer scrutinized the export attack surface;
  `ViolationReportExporter` byte-unchanged. Extends ADR-013/015 (no new ADR).
- **PR #203 (squash `3d19f98`) - persist Audit filters + sort.** Survive step-reopen + relaunch
  via 5 forward/back-compat `UiState` init props + a `UseUiStateStore` seam (restore + persist-on-
  change, no rail/theme clobber). Graph-lens-across-relaunch deferred (already persists in-session).
  +19 tests; reviewer ruled the UiState-list equality test-narrowing correct (serialization-boundary
  artifact, no prod consumer).
- **PR #204 (squash `c481d92`) - per-row Audit triage.** Surfaced the existing AcknowledgeRow/
  SuppressRow/UntriageRow via detail-pane buttons + a row context menu (AXAML only). +6 tests incl.
  a runtime proof the context-flyout `$parent` commands resolve from the popup. ADR-028 governs.
- **PR #205 (squash `a248b1f`) - Plan keep-alive (silent-data-loss fix).** The plan survives Back
  (mirrors ADR-014 + reuses the ADR-025 parking lot) - re-enter same instance over the same base OU,
  rebuild on a different one; new "New plan" reset; header reflows to a WrapPanel (ui-verifier caught
  + I fixed a title-squeeze/clip). +7 tests; 2 parking-lot tests inverted to the keep-alive truth.
- **PR #206 (squash `7cfad34`) - theme System/auto.** Third theme choice following the OS light/dark
  preference: AppThemeChoice enum + injectable IPlatformThemeProvider (reads PlatformSettings, never-
  throws; RequestedThemeVariant=null does NOT auto-follow); resolves System for native chrome + graph
  canvas; subscribes to OS changes only while System (unsubscribes on Dispose); 3-state toggle glyph
  ☾/☀/◐ + tooltip. bool IsLightTheme -> enum (8 screenshot tests renamed). +new tests. Extends ADR-026.
- **PR #207 + #208 (squash `5e6c397`, `7a56129`) - Settings editor completeness (2 slices).** A:
  naming rule Add/Remove, editable ruleset metadata header, read-only matrix legend. B: validation-band
  rows navigate to the offending tab (path->tab + two-way SelectedTabIndex; Message stays plain #45);
  ignore/exception rows get a live glob preview (MatchEntryEditor.PreviewCandidate, UI-only, Build()
  omits it). +30 tests. Follows ADR-011/008 (no new ADR). Note: empty-string id/pattern/name is a
  pre-existing loader lenience (empty pattern is inert) - out of scope to harden here.
- **PR #209 (squash `7261224`) - finding-row interaction unification (Tier 1).** Shared
  `ListBottomFadeMask` "more below" fade on all four findings lists; Plan findings rows now
  jump-to-graph command Buttons with the IsActive highlight (reused SelectedDn/GraphRenderer). +5
  tests. Deferred (Tier 2): audit checkbox-vs-select disambiguation + a unified hover-wash.
- **PR #210 (squash `8c77c38`) - ADR-034 native motion: DEFERRED.** A gated step-swap cross-fade was
  spiked + reverted - a `TransitioningContentControl` keeps the leaving step view alive during the
  fade, regressing the ADR-025 parking-lot lifecycle (2 lifecycle tests failed with the fade forced
  ON). Docs-only ADR records the settled reduced-motion approach (SPI_GETCLIENTAREAANIMATION seam +
  motion tokens) + the blocker + the failing tests; no code lands (avoids dead code). Rail animation
  also deferred (GridLength can't tween).
- **PR #211 (squash `4cbd2d0`) - discoverability (final WP).** Double-click-to-expand affordance
  (External-node pointer cursor + legend hint), an "Expand selected node" palette action (reuses the
  nodeExpand wire), and a keyboard-help "?" modal listing both keyboard sets. build + graph-bundle
  gates green; ui-verifier caught + I fixed a legend-hint contrast fail (moved to the AA-clearing
  edge-text token). Deferred: the analysis-mode grouping redesign.

## Decided

- **Token/vocabulary adoption is a correctness gate, not polish.** The audit's top lever was an
  off-token error red below AA on the light theme - a silent regression of the v0.2 guarantee,
  not a new feature. Shipped as quick-wins; no ADR needed (presentation within existing bounds).
- **A high-risk-low-value slice that threatens a delicate subsystem gets deferred with a documented
  ADR, not forced.** Native motion's cross-fade regressed the ADR-025 parking lot; rather than
  re-engineer that load-bearing lifecycle for a subtle polish, ADR-034 records the approach +
  constraint for a future cohesive slice. Unwired infrastructure is dead code - land it with its
  consumer, not speculatively.

## Next

- **The 2026-06-30 whole-journey UX audit roadmap is COMPLETE** - 14 WPs auto-forwarded end-to-end
  (#199-#211), each branch -> PR -> green CI -> squash-merge through the subagent pipeline. Small
  documented deferrals for a future slice: native motion (ADR-034; the step-swap cross-fade +
  rail animation both blocked/deferred - reduced-motion seam + a parking-lot-safe transition), the
  audit checkbox-vs-select disambiguation + a unified finding-row hover-wash (Tier 2), and the
  analysis-mode grouping redesign. No release cut this session (no tag); consider a `cut-release`
  once these UX slices bake. Plan: `~/.claude/plans/create-a-plan-floating-milner.md`.

---

# 2026-06-30 - Session 38 (UX-fit-audit levers 1-3 shipped -> #188/#189/#190 closed)

**Phase:** Phase 4 (feedback-driven). Implemented all three Proposed ADRs from the
2026-06-29 UX fit-audit end-to-end through the subagent pipeline (test-engineer ->
implementer -> reviewer -> ui-verifier -> ci-sentinel), each as branch -> PR -> green CI ->
squash-merge. Auto-forwarded `030 -> 031 -> 032`; a `/loop` "go forward if the slice is
correct" heartbeat gated each merge on all gates being green.

## Done

- **#188 / PR #192 (squash `352af93`) - ADR-030 band severity-gate.** `AuditSummary.BandFor(
  score, critical, warnings)`: `Critical>0` -> new "Action required" band at any score; a
  `Warning` caps below Excellent; score/weights/penalty math unchanged. Triage disclosed
  App-side (would-be vs live) beside the band + in the HTML export header. 802 App + 770 Core.
- **#189 / PR #193 (squash `ecccf3d`) - ADR-031 connection targeting.** Optional Connect
  "Advanced" disclosure (server/DC host + base DN) under integrated auth - no credential field
  (E2), `runas /netonly` for alternate accounts - with a pre-bind target line; whole-domain
  root candidate; `ConnectionTarget` validators (server = bare host only; baseDn = search-base
  only, never a filter). Folded in the "1 finding" pluralization fix. 820 App + 808 Core.
- **#190 / PR #194 (squash `9da82ed`) - ADR-032 audit run history + drift.** `AuditRun` + pure
  `AuditRunDiff.Compute` (Fixed/New/Still-open/Now-unchecked + RulesetHashMismatch);
  `AuditRunStore` under `%APPDATA%\runs\` (atomic, never-throw, hardened JSON); Save/Compare on
  the Audit screen with unchecked-honesty + ruleset-mismatch banners. 830 App + 820 Core.
  (ui-verifier caught a 4.44:1 light-theme tile caption; fixed to 5.25:1 before merge.)

## Decided

- **Honesty is a correctness gate, not polish - at every surface.** A green band over a live
  Error (030), a single-domain ceiling the Core could already lift (031), and "vanished ==
  fixed" drift (032's Now-unchecked tri-state) are all trust defects, not features; each ADR
  keeps the read-only / E2 / unchecked-tri-state invariants intact. ADR-030/031/032 -> Accepted.

## Next

- Levers 4-5 remain unfiled: seed-from-Ist (Plan/Gap usability, AP 4.2.3 designed) + a
  consolidated management report. Cheap wins outstanding: Gap CSV/HTML export, RootPicker
  same-name DN disambiguation, wire the dead rail severity chips, surface WhyItMatters on the
  drill path.

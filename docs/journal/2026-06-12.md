# 2026-06-12 — Session 3 (Phase 1 complete → M1)

**Phase:** Phase 1 (Fundament) done, milestone **M1** reached. Next phase: 2 (Graph-View).

- **Done:** All five work packages as reviewed squash-merged PRs: AP 1.1 repo
  polish (#10), AP 1.2 sln scaffold + build.ps1 gate + CI (#11, first runs green),
  AP 1.3 core data model (#13, 40 tests, contracts in .claude/rules/data-model.md),
  AP 1.4 DemoProvider + embedded 194-object dataset mirroring the lab fixture
  (#15), AP 1.5 read-only LdapProvider (#18: paging, ranged retrieval, FSP→External,
  13-attribute whitelist as PropertiesToLoad, ADR-002 no-primaryGroupID-edge,
  foreign-SID FSP lab fixture via ad-fixture-admin). 161 tests live / 139 in CI.
  **M1 DoD verified:** `connected, 88 groups loaded` (LDAP, live DC) and
  `connected, 40 groups loaded` (demo), both exit 0; CI green on main.
- **Decided:** ADR-002 (primaryGroupID stays attribute-only); App is a console
  stub until AP 2.1 (commit body 59c3c3b); well-known S-1-5-11 is SAM-rejected
  for account-domain groups → fixture uses a fabricated foreign-domain SID
  (quirks in .claude/rules/lab-environment.md). TDD caught a real bug:
  unmapped 0x80072032 ERROR_DS_INVALID_DN_SYNTAX (fixed + pinned).
- **Open/follow-ups:** #12 (CI actions → Node 24 majors before 2026-06-16!),
  #14 (built-in imitations vs AP 3.1 ignore list), #16 (DN escaping in PathFor
  BEFORE the AP 2.1 picker), #17 (narrow LoadScope NotFound fallback). GitHub
  Project board still blocked: PAT lacks `project` scope (human: `gh auth
  refresh -s project,read:project`, see #5).
- **Next:** Phase 2 — AP 2.1 app shell (WebView2 startup check, OU picker;
  fix #16 + #12 first), then AP 2.2 graph rendering per ADR-001 (GraphSpike
  gotchas in .claude/rules/lab-environment.md bind here).

# 2026-06-12 — Session 4 (hook hotfix)

- **Done:** All three hooks in `.claude/settings.json` were dead — Claude Code
  runs hook commands through Git Bash on Windows, which expanded `$env:…` and
  `$LASTEXITCODE` to empty before pwsh ran. Fixed to
  `pwsh -NoProfile -File "$CLAUDE_PROJECT_DIR/…"` (POSIX expansion, native exit
  propagation); guard matrix re-verified end-to-end (block=2 / pass=0), the
  live harness hook even blocked a test payload. Rule updated in
  `.claude/rules/lab-environment.md`; reviewer approved.
- **Also:** `C:\Program Files\Git\bin` (bash/sh) appended to the Machine PATH
  on user request — mirrored as idempotent bootstrap.ps1 step 2b (CLAUDE.md:
  machine reconfiguration must survive a rebuild), full bootstrap re-run green,
  lab-environment rule updated, reviewer approved.

# 2026-06-12 — Session 5 (Phase 2 start: AP 2.1 done)

**Phase:** 2 (Graph-View) — AP 2.1 merged; next AP 2.2.

- **Done:** Phase-2 issues #19–#23 created. #24 fixed #16+#17 (AdsPath escapes
  `/` in ADsPaths — host-redirect shape neutralized, live regression fixture
  `OU=Research/Development` seeded, lab now 195 objects/5 OUs; LoadScopeAsync
  probes the base DN, mid-load races stay honest null). #25 closed #12 (CI
  actions → Node-24 majors, 4 days before the flip). #26 closed #19: AP 2.1
  App-Shell in 5 slice commits — Avalonia WinExe (net8.0-windows, app.manifest),
  M1 contract lives on as `--check`, connection dialog, root picker as
  Pflichtfilter, WebView2 registry-probe banner, GraphHost/DetailPanelRegion
  seams. Gate 222/222 incl. live AD; ui-checklist B 26/26; all PRs reviewer-
  approved; CI green throughout.
- **Decided:** ADR-003 (CommunityToolkit.Mvvm 8.4.2, Avalonia exact 11.3.17,
  single step-switched window, manual composition root, DirectoryUnavailable-
  only-at-VM-boundary); WebView2 probe per registry not SDK (commit body);
  kind-badge palette in AdObjectKindConverters binds AP 2.2 graph colors.
- **Gotchas → rules:** DPI-aware PrintWindow capture; headless
  CaptureRenderedFrame lags one compositor batch (lab-environment.md).
- **Next:** AP 2.2 graph rendering (#20): vendor Cytoscape 3.34.0, WebView
  11.4.0, GraphBuilder in Core (Edges read once!), concentric layout, Playwright
  part-A verification. Open: #14 (built-in imitations), Project board PAT scope.

# 2026-06-12 — Session 6 (AP 2.2 done: the graph is live)

**Phase:** 2 (Graph-View) — AP 2.2 merged (#27 → 80671fb, closes #20); next AP 2.3.

- **Done:** ADR-004 + 7 implementation slices, all TDD where it counts: Core
  GraphBuilder (DN-depth × kind-rank rings, chord-formula no-overlap proof,
  External materialization, Edges read ONCE — 57 tests incl. 4 live-AD),
  vendored web bundle (Cytoscape 3.34.0 SHA-pinned, getElementById-only),
  wire contract + chunker + `--demo --dump-graph` (demo-only guard, exit 64),
  Playwright harness as the same gate locally + CI (`tools/test-graph-bundle.ps1`),
  workspace load flow behind IGraphRenderer (null factory ⇒ headless-safe),
  CytoscapeGraphRenderer on WebView [11.4.0] — windowed smoke verified the full
  JS→.NET→VM roundtrip on the box. ui-verifier failed A2 once (containment
  contrast 1.13:1 → fixed to 2.45:1, re-judged PASS); reviewer approved.
- **CI saga (3 red runs before green):** (1) verify.mjs hung 3 h — page.evaluate
  has NO Playwright default timeout; (2) hardening (watchdog/phase logs/crash
  handlers/step-timeout 12 m) pinpointed it: the dbltap probe RETURNED the
  cytoscape collection — Playwright serializing that cyclic monster wedges the
  2-core runner. Braces fixed it. (3) publish-survival check's 500KB floor was
  wrong (cytoscape.min.js is ~425 KB) → SHA256-vs-source comparison instead.
  Morals → harness: never return rich page objects from evaluate; bound every
  CI step; no magic-number sanity floors.
- **Gotchas → rules:** no interactive input desktop in agent context (drive UIA
  patterns + posted WM_* to the Chromium child HWND); once the WebView HWND
  exists, UIA sees only Chromium content (lab-environment.md).
- **Follow-ups filed:** #28 (pin ASCII-only wire output), #29 (DemoProvider
  IsInScope EndsWith vs escape-aware DnPath drift). GitHub notice: windows-latest
  → windows-2025-vs2026 redirect by 2026-06-15 (no action; watch first runs).
- **Next:** AP 2.3 lazy expand (#21; nodeExpand protocol + renderer counter
  seams are ready). Open: #14, Project board PAT scope (human:
  `gh auth refresh -s project,read:project`).

# 2026-06-12 — Session 7 (AP 2.3 + 2.4 + 2.5 done → **M2 reached**)

**Phase:** 2 (Graph-View) **COMPLETE** — AP 2.3 (#31 → 091c34b), AP 2.4
(#33 → 9c39de4), AP 2.5 (#35 → 64fc9b1), M2 evidence (#36 → 478ee77).
Next: Phase 3, AP 3.1 (rule model).

- **Done:** ADR-005 + 6 implementation slices (orchestrated workflow, TDD pairs):
  `graphUpdate` replace-in-place commit verb (one cy.batch on the live instance —
  viewport preserved; diff protocol unsound on per-build edge ids, add-only would
  void the no-overlap proof), renderer seam `UpdateGraphAsync`/`FocusAsync` (shared
  single-flight + 60 s bounded-wait), VM expand pipeline (fetch only at the
  out-of-scope frontier: kind ∈ {GG,DL,UG,External} ∧ !IsLoaded; OUs/users get
  focus only — null-vs-empty contract never fabricated), Refresh = forced expand
  of SelectedDn (detail-column header). 409 tests incl. live AD (A↔B cycle
  re-expand/refresh terminates, FSP frontier → loaded-and-empty), Playwright 48
  asserts (instance identity, viewport 1e-9, fresh-page jsError probe),
  ui-verifier PASS; windowed smoke grew 3→22 nodes with pan preserved.
- **Review cycle (1 reject → fix → approve):** busy gate didn't span the
  focus-only path — overlap could fault the renderer single-flight, worst case
  drawn graph silently diverging from snapshot. Fixed: IsLoading spans the whole
  pipeline (drop-not-queue pinned for the focus path too); null-renderer guard
  on Refresh (AP 2.5 sets SelectedDn without a renderer); focusOn listener order.
- **Decided:** ADR-005 (protocol, cache policy, refresh semantics); whole-scope
  reload + orphan ex-member pruning deferred → #30; renderer TCS sequence
  correlation deferred as documented nit (gate now prevents VM-originated overlap).
- **AP 2.4 (same session):** exploration proved NO transitive membership
  traversal existed anywhere in src/ (providers flat, expand one-level, DN
  ancestry acyclic) — so the package shipped the missing Core seam per ADR-006:
  `MembershipTraversal.Walk` (iterative DFS, white/gray/black via Dn.Comparer,
  GetMembers-only adjacency, cycles as back-edge path VALUES, frontier =
  never-loaded fetchable kinds → "unexpanded areas unchecked" made literal).
  15 pins incl. diamond-not-a-cycle, case-variant closing edge, self-membership,
  live-lab cycle-hidden-behind-frontier-until-expanded. Rules bullet makes Walk
  the only sanctioned walk for AP 3.2/3.4. Reviewer approved first pass (two
  doc-wording findings fixed pre-push).
- **Filed:** #32 — runner image flip windows-latest→windows-2025-vs2026 on
  2026-06-15; CI annotates every run. Plan: pin `windows-2022` (matches the
  Server-2022 dev box) before the flip.
- **AP 2.5 (same session):** ADR-007 — snapshot-only reads (zero provider
  traffic on click), immutable DetailPanelModel projection as single binding
  choke point (whitelist structurally enforced: x:DataType = projection, rows
  mirror Attributes verbatim — UI never re-filters so provider bugs SURFACE).
  Fixture gap closed en route: seed-testad gained idempotent department/title
  top-up (140 users updated, lab mirrors demo dataset), live pins strengthened
  to presence+exact values. Reviewer approved first pass; 452 tests.
- **M2 close-out (#36):** demo-mode exploration GIF (960×627, 21 frames,
  463 KB) in README via new tools/record-demo-gif.ps1 (UIA + posted WM_*,
  ffmpeg palettegen; ffmpeg → bootstrap.ps1). Reviewer REJECTED take 1: the
  idle connect card rendered the live "AGDLP\Administrator" identity into 2
  frames (public-media rule) — branch was unpushed, commit replaced, blob never
  reached the remote; take 2 starts at the root picker (demo click stays
  off-camera but structural). Lesson → recorder header + rules: cytoscape
  discrete-wheel normalization (delta magnitude ignored, post per-detent).
- **CI:** runner pinned to windows-2022 (#34, closes #32) before the
  windows-latest→windows-2025-vs2026 flip on 2026-06-15; image verified
  provisioned. Workflow-agent note: one 529-Overloaded mid-run — Workflow
  resume with resumeFromRunId replayed cached slices cleanly.
- **Next:** Phase 3 — AP 3.1 rule model (decide #14 built-in imitations as
  part of it). Open: #14, #28, #29, #30, Project board PAT scope (human:
  `gh auth refresh -s project,read:project`).

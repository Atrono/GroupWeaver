# 2026-06-11 — Session 1 (bootstrap)

**Phase:** Phase 0 (spike: AP 0.1 WebView graph, AP 0.2 LDAP, then ADR-001) — bootstrap fully complete

- **Done:** Chocolatey + toolchain installed (git, gh, dotnet 8.0.421, node LTS,
  WebView2 Runtime); repo initialized locally; `.claude/` scaffolded (7 agents,
  3 hooks, 2 skill stubs); `tools/bootstrap.ps1` + `tools/seed-testad.ps1`
  authored, adversarially verified (13-agent workflow), all confirmed findings
  fixed; DC promotion to new forest `agdlp.lab` triggered at session end (box
  reboots, session dies — expected per CLAUDE.md).
- **Decided:** project identity is the pseudonym **Atrono** everywhere (user
  instruction; saved to auto memory); hook commands resolve scripts via
  `$env:CLAUDE_PROJECT_DIR`; `seed-testad.ps1` pins every write (incl.
  memberships) to the lab OU via `-SearchBase` + DN assertion.
- **Post-reboot (same day):** DC verified (agdlp.lab, DomainRole 5, LDAP 389 up);
  human resolved gh auth (account "Atrono") and pre-created the public repo —
  local history rebased onto its initial commit and pushed (blocker file deleted);
  fixtures seeded via `ad-fixture-admin`: 194 objects (140 users, 40 groups,
  10 computers, 4 OUs), circular nesting + all deliberate violations + empty
  groups verified read-only.
- **Next:** Phase 0 spike — AP 0.1 (5k-node graph in Avalonia-hosted WebView2
  incl. bidirectional event roundtrip), AP 0.2 (DirectorySearcher vs. lab AD),
  then ADR-001. Note: seed summary counts memberships as "created" (333 vs. 194
  objects) — cosmetic, fix opportunistically.

# 2026-06-11 — Session 2 (Phase 0 complete → M0)

**Phase:** Phase 0 done, milestone **M0** reached (PR #4 squash-merged, 249bd3a).
Next phase: 1 (Fundament).

- **Done:** Fixed two session-error sources first (user request): statusline was
  sh/jq-based (neither on PATH) → PowerShell-native rewrite; PreToolUse guard
  never blocked because `pwsh -Command "& script"` swallowed exit 2 → wrappers
  now `exit ($LASTEXITCODE ?? 1)` (afb557c). Then Phase 0: issues #1–#3, AP 0.2
  LdapSpike (194 objects ~100 ms paged, 40 groups transitive ~1 ms, A→B→A
  terminates), 8-agent verified research (49/50 claims), AP 0.1 GraphSpike all
  6 criteria PASS, ADR-001 Accepted, reviewer-approved, issues closed w/ notes.
- **Decided:** ADR-001 — Cytoscape.js 3.34.0 vendored in Avalonia.Controls.WebView
  11.4.0 (official, MIT since 2026-03), canvas renderer only, chunked InvokeScript
  transfer (WebResourceRequested can't serve responses in 11.4.0), MSAGL+Skia as
  exit strategy. GPU driver (UHD 620) landed mid-spike → both rendering
  environments measured; software-rendered floor preserved (37.5 FPS gestures).
- **Next:** Phase 1 — AP 1.1 (repo polish: README skeleton, THIRD-PARTY-NOTICES,
  issue templates, GitHub Project), AP 1.2 (CI), AP 1.3 (data model). Durable
  box facts now in .claude/rules/lab-environment.md (GPU-driver history, no
  sh/jq, hook exit-code rule, GraphSpike gotchas binding for Phase 2).

# 2026-06-17 — Session 20 (open-points backlog: #79 + #77 cleared, #78 blocked, #54 deferred)

**Phase:** Phase 4. Wrote an execution plan for the open points
(`docs/superpowers/plans/2026-06-17-open-points-backlog.md`, via the
superpowers:writing-plans skill) then executed it subagent-driven, in
urgency order. Open before: #54, #77, #78, #79. Open after: #54, #78.

- **#79 (CLOSED, PR #80) — release-blocking, done first.** `release.yml` pinned
  `actions/attest-build-provenance@v2.4.0`, a composite whose inner
  `actions/attest` ran on the **Node 20** runtime GitHub removed 2026-06-16.
  Bumped to **v3.0.0** (`977bb373…`; inner attest = node24), `subject-path` I/O
  unchanged. Verified every other pinned action (checkout v6.0.3, setup-dotnet
  v5.3.0, cache v5.0.5, upload-artifact v7.0.1) is ALREADY node24 and `ci.yml`
  uses no attestation. Chose v3.0.0 over v4.x: minimal Node-24 hop, no new
  `create-storage-record` behavior. End-to-end proof (gh attestation verify)
  deferred to the next tagged release — did NOT cut a throwaway release.
- **#77 (CLOSED, PR #81) — author-time char parity.** `PlanModel.AddNode`
  rejected only ASCII control chars and `RenameNode` rejected NONE, while
  `PlanScriptExporter.Guard` rejected the wider set (control + U+2028/U+2029 +
  the U+2018–U+201F curly-quote block from the 0.2 audit). Extracted the
  predicate to shared `PlanText.IsUnsafe`/`ContainsUnsafe`; routed AddNode,
  RenameNode, and Guard through it (can't drift again). Smart-quote names now
  fail fast in the editor (`EditError`). TDD: test-engineer red → implementer
  green. Plan's B10 was wrong (two exporter theories delivered the unsafe char
  via the Name channel through AddNode, which now rejects it first) → added a
  `ForceName` direct-injection helper (mirror of `ForceSam`) to preserve the
  exporter's name-token Guard coverage without weakening AddNode. **1255 tests,
  0 failed, 0 skipped** (incl. live RequiresAd fixtures).
- **#78 (OPEN, PR #82 = partial fix + blocker doc) — stuck rule invoked.** The
  app renders the graph correctly under the dark theme (frames confirm), so the
  visual goal is reachable, but `record-demo-gif.ps1` is blocked at beat 3: the
  posted `WM_LBUTTON` to the WebView `Chrome_RenderWidgetHostHWND` doesn't
  register as a cytoscape node tap. Ruled OUT: stale captures (real bug — fixed
  via new `Capture-Live` double-capture defeating the PrintWindow one-batch lag
  — but NOT the click cause) and DPI (display is 100% scale, capture 1:1 with
  the window rect). 2 approaches + root-cause → `BLOCKED-demo-gif-dark.md`. Low
  urgency: the README hero already uses the committed dark stills.
- **#54 (OPEN) — left design-gated, by design.** Graph-layer reachability
  pruning breaks GraphBuilder totality + creates graph/report node-set
  divergence + re-opens the geometry proof → needs ADR-016 FIRST and is
  explicitly "pick up only if user feedback shows the node-Refresh residual
  matters." The plan documents the ADR decisions to resolve; wrote NO code.
- **Gate:** every merged PR (#80/#81/#82) got reviewer APPROVE + green CI via
  ci-sentinel; squash-merged, branches deleted. Out of plan scope (recorded):
  O5 winget (needs a Win-11 VM), v0.4 Entra ID (needs brainstorming), ADR-015
  extensions (DiffStatus.Modified, Cross-Scope-Rebase).
- **Next:** #78 — fix the cytoscape-tap injection (tap-vs-drag and legend-overlay
  leads are in the BLOCKED note); #54 only if user feedback justifies it.

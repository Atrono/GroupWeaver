# 2026-06-21 — Session 21 (v0.2 polish pass — Phase 0 done: tooling + audit)

**Phase:** Phase 4. A brainstorming dialogue resolved the open fork *"optimize UI/UX
or build v0.4?"* → **polish the shipped v0.2 product; do NOT start v0.4** (Entra
ID/M365 breaks E2 on-prem/integrated-auth, no user pulling for it). Approach **A,
audit-first**, across visual identity / interaction-feel / edge-state UX, anchored to
declared standards (brand tokens, WCAG 2.2 AA, Nielsen) — the repo had good instincts
but none declared. Spec: `docs/superpowers/specs/2026-06-21-v0.2-polish-pass-design.md`.

- **Phase 0 motion tooling (#85, PR #86, merged 3dc7faf, CI green).** Built
  `tools/capture-motion.ps1` (in-process high-fps PrintWindow burst recorder — ~13fps
  on this box, PrintWindow is the bottleneck; assembles clips at the MEASURED rate so
  playback is honest), `tools/check-contrast.ps1` (deterministic WCAG report), and
  `tools/lib/webview-capture.ps1` (shared lib extracted from `record-demo-gif.ps1`,
  which now consumes it, −217 lines, re-verified no GIF regression). reviewer APPROVE.
- **Phase 0 audit (#87–#93 filed, doc `2026-06-21-polish-audit.md`).** Rendered all 37
  surfaces + motion frames, judged via two parallel `frontend-design`-lensed agents.
  Verdict: passes correctness, **lacks identity + entirely static** (no hover/selection
  feedback, expand SNAPS, focus-fit CUTS). **WCAG measured:** DL/UG/Computer node fills
  fail 1.4.11 (<3:1); **white-on-Warning badge 2.06:1** = real bug. **Identity-ADR gate
  DECIDED:** ADR needed for ONE slice only (#90 design-tokens WCAG re-tone); all else
  (signature, motion, type, layout) ships without it. **Signature direction:** render
  the AGDLP concentric structure as faint named "tier shells" (#87).
## Phase 1 — slice 1 shipped: #88 graph motion (PR #95, merged a1fb5b4, CI green)

Effort flipped to **ultracode** (xhigh + Workflow orchestration). Ran a design
**Workflow** (5 parallel code/contract readers → synthesis → adversarial critique)
before touching code — its critique returned **revise** and caught three real traps:
cytoscape element `opacity` composites a node's OWN halo/underlay (so a new+flagged
node's halo fades in with it — accepted, but the test must read survivors only);
ADR-005's full-relayout means **survivors already jump** on every expand (accepted,
pre-existing); and F12 needs an IGraphRenderer change → **split to #94**. So #88
shipped **F1+F2 only** (pure JS, no C# change):

- **F2 eased focus-fit** — `focusOn` animates the camera (cy.animate fit 280ms),
  `focused` moved to the animate `complete` (empty/un-pannable + reduced-motion keep
  the instant path so it still posts exactly once — no FocusAsync-timeout risk).
- **F1 new-node enter fade** — only genuinely-new nodes (diffed vs pre-removal id set)
  fade element opacity 0→1 (240ms); survivors replace instantly; ADR-005 D1 untouched.
- **ADR-017** records it; **ui-checklist** §A elevated; `verify.mjs` gains isolated
  camera/enter counters + a reduced-motion probe (**183 asserts**, green).
- **TDD** (test-engineer red → implementer green; one mid-tween read needed a
  `waitForFunction` settle poll, NOT a graph.js weakening). **ui-verifier PASS**
  (fade caught mid-tween in frame a_08), **reviewer APPROVE**, build green (1255).

## Phase 1 — slice 2 shipped: #89 graph interaction feedback (PR #97, merged 64b218b)

Same design-Workflow-first cycle. Critique caught two BLOCKERS pre-code: cytoscape has
no `background-brighten` (only `background-blacken`, negative=brighter) → the brightness
cue would have silently no-op'd; and synthetic headless `emit('tap')` doesn't run native
select → `applySelection` must call `node.select()` explicitly. Shipped:

- **Selection + neighborhood dim** (tap → white border + dim non-neighbors via
  `background-blacken`; closed neighborhood stays bright; background-tap clears),
  **hover** (brighten/border), **selective labels** (root + Error always-labeled at fit).
- **Channel-ownership contract (ADR-018):** every cue rides a channel DISJOINT from the
  occupied ones (opacity=#88 fade, overlay=severity, underlay=diff) — so a dimmed node
  KEEPS its severity halo (ui-verifier measured 54.5 vs 54.4 redness, unchanged) and the
  #88 motion counters stay 0 (instant toggles). nodeClick contract untouched.
- Client-only graph.js (no C#); 218 graph-bundle asserts + 1255 tests green; ui-verifier
  PASS, reviewer APPROVE. Reverse sidebar→graph select-sync split to **#96**.

## Phase 1 — slice 3 shipped: #87 encoding-key signature (PR #98, merged 0f2a724)

The signature slice — and the design workflow's **premise-check earned its keep**: it
REFUTED the audit's AGDLP-onion premise (graph radius = OU-containment depth, NOT kind-tier;
ADR-004 D1 — the same kind sits at many radii, so tier rings would be a *lie*). Consulted the
user with a UI-design/psychology rationale (perceptual budget, figure-ground, expert-trust,
von Restorff); they chose **redirect the signature to the encoding language made legible.**

- Recast the static text legend into a crafted 4-section **KEY**: KIND rows with inline
  real-shape SVG swatches mirroring the canvas nodes (pinned hexes + uniform hairline for
  WCAG 3:1) + right-aligned tabular **live per-kind counts** (`cy.nodes()`, refreshed at the
  `sendLoaded` chokepoint on commit AND lazy-expand — External self-corrects); SEVERITY +
  DIFF as key-only glyphs (no counts — would contradict the sidebar tally). Fixed the
  contains-edge sample colour drift (#4A5568→#7A8699).
- JS/HTML only, no C#/wire change; 273 graph-bundle asserts (legend = a 4th palette parity
  tripwire) + 1255 tests; **ui-verifier PASS** (7 swatches distinct by shape+hue at 16px —
  the top taste risk, cleared; one DIFF-row clip found+fixed by widening the panel),
  **reviewer APPROVE**. Audit doc records the refutation honestly. ADR-018 unchanged.

**Phase-1 scorecard (re-evaluate-leverage checkpoint):** #88 motion ✅ · #89 interaction ✅ ·
#87 signature ✅ — three merged, each design-grounded → TDD → adversarially verified.
- **Remaining backlog:** #91 type scale · #92 workspace layout/clip · #93 connect step ·
  **#90** design-tokens ADR + WCAG re-tone (carries the white-on-Warning **bug** — highest
  correctness value). Follow-ups: #94 (F12 busy ring), #96 (reverse select-sync), legend
  click-to-filter. Leverage still high; next natural batch is the native-chrome slices
  (#91/#92/#93) + the #90 token ADR.

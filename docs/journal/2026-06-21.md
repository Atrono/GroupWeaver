# 2026-06-21 — Session 21 (v0.2 polish pass — Phase 0 done: tooling + audit)

**Phase:** Phase 4. A brainstorming dialogue resolved the open fork *"optimize UI/UX
or build v0.4?"* → **polish the shipped v0.2 product; do NOT start v0.4** (Entra
ID/M365 breaks E2 on-prem/integrated-auth, no user pulling for it). Approach **A,
audit-first**, across visual identity / interaction-feel / edge-state UX, anchored to
declared standards (brand tokens, WCAG 2.2 AA, Nielsen) — the repo had good instincts
but none declared. Spec: `docs/superpowers/specs/2026-06-21-v0.2-polish-pass-design.md`.

- **Phase 0 motion tooling (#85, PR #86, merged 3dc7faf, CI green).** Built
  `tools/capture-motion.ps1` (in-process high-fps PrintWindow burst recorder — ~13fps
  on this box, PrintWindow is the bottleneck; assembles clips at the MEASURED rate so
  playback is honest), `tools/check-contrast.ps1` (deterministic WCAG report), and
  `tools/lib/webview-capture.ps1` (shared lib extracted from `record-demo-gif.ps1`,
  which now consumes it, −217 lines, re-verified no GIF regression). reviewer APPROVE.
- **Phase 0 audit (#87–#93 filed, doc `2026-06-21-polish-audit.md`).** Rendered all 37
  surfaces + motion frames, judged via two parallel `frontend-design`-lensed agents.
  Verdict: passes correctness, **lacks identity + entirely static** (no hover/selection
  feedback, expand SNAPS, focus-fit CUTS). **WCAG measured:** DL/UG/Computer node fills
  fail 1.4.11 (<3:1); **white-on-Warning badge 2.06:1** = real bug. **Identity-ADR gate
  DECIDED:** ADR needed for ONE slice only (#90 design-tokens WCAG re-tone); all else
  (signature, motion, type, layout) ships without it. **Signature direction:** render
  the AGDLP concentric structure as faint named "tier shells" (#87).
- **Next:** Phase 1 execution, cheapest-highest-feel first — **#88 eased focus-fit**
  (S, transforms the feel) → #89 selection/hover → #87 signature → #91 type scale →
  #92 layout → #93 connect → #90 ADR+tokens (carries the white-on-Warning bug, don't
  defer far). Re-evaluate leverage every 2–3 slices; stop when it drops.
